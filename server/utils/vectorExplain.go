package utils

// What a finding from a given tool actually means, as hard-coded reference attached to every
// matching result.
//
// GENERATED CONTENT, hand-reviewed. Every entry was drafted against the tool's own parser and
// documentation and then put through an adversarial pass whose only job was to find overclaiming.
// That pass changed real things: dalfox V was rewritten because its detection_method distinguishes
// a PARSED-position judgement from an out-of-band callback, and only the second is execution.
//
// The field this file exists for is WhatItDidNotProve. The gap between what a tool observed and
// what a reader will assume it proved is where this project has repeatedly been misled, so every
// entry has to state it and a test enforces that.
//
// Keyed by tool and kind, with a "*" entry per tool so an unenumerated kind degrades to something
// honest rather than to nothing.
//
// The access bypass and prototype pollution entries were TRIMMED BY HAND after review. The
// generated versions ran to twelve and twenty-one validation steps, most of which were triage and
// reporting advice rather than validation. An explanation nobody finishes reading protects nobody.

// FindingExplanation is the reference text shown beside a finding.
type FindingExplanation struct {
	Title             string   `json:"title,omitempty"`
	WhatItProved      string   `json:"what_it_proved,omitempty"`
	WhatItDidNotProve string   `json:"what_it_did_not_prove,omitempty"`
	WhyItMatters      string   `json:"why_it_matters,omitempty"`
	FalsePositive     string   `json:"false_positive_looks_like,omitempty"`
	SeverityNote      string   `json:"severity_note,omitempty"`
	ValidationSteps   []string `json:"validation_steps,omitempty"`
	References        []string `json:"references,omitempty"`
}

// ExplainFinding looks up the reference for one finding, falling back to the tool's default.
func ExplainFinding(tool, kind string) FindingExplanation {
	if entry, ok := findingExplanations[tool+"|"+kind]; ok {
		return entry
	}
	if entry, ok := findingExplanations[tool+"|*"]; ok {
		return entry
	}
	return FindingExplanation{}
}

var findingExplanations = map[string]FindingExplanation{
	"dalfox|*": {
		Title:             "Dalfox, kind not yet catalogued: read the type letter, the detection_method and the evidence suffix as three separate axes",
		WhatItProved:      "Dalfox v3 emits four finding types and the framework stores the letter verbatim in kind, so any letter beyond V, R and A is one of dalfox's own with its own meaning. From dalfox 3.2.1's documented model: V is \"Dalfox asserts this input is exploitable. Act on it.\" R is \"The payload came back in the response, but its position was not confirmed exploitable.\" A is transitional wording, a detection-method label rather than an exploitability tier. I is \"Not an XSS claim at all\", produced only when --detect-outdated-libs is on, mapping to CWE-1104 rather than CWE-79; that flag is an operator setting in this framework and is off by default. Whatever the letter, three stored fields are independent and all three should be read: kind (the type letter), detection_method (one of reflection, dom-verification, ast, oob, library), and the parenthesised suffix at the end of evidence, which names the specific verification or decoding class. One fact overrides every letter: dalfox v3 ships NO headless browser of its own, and dalfox's own guidance is to avoid the word \"executed\" for anything except detection_method \"oob\".",
		WhatItDidNotProve: "The type letter is not stable. Dalfox's integrator guidance is explicit that AST findings should be selected by detection_method == \"ast\" rather than type == \"A\" because the method field is stable and the A tier is being folded into the confidence axis. So treat an unfamiliar letter as unclassified rather than assuming it inherits V's weight. The severity and confidence on the row are dalfox's, stored unchanged, and confidence is only ever high or low with confidence_reason naming the deciding signals; neither is a measurement of impact. A detection_method of \"oob\" means a blind payload reached a collector, which proves out-of-band execution somewhere but does not tell you which request, which user, or which origin, and it only happens if the operator set -b or --blind-oob. A detection_method of \"library\" is a version fingerprint from a <script> tag and involves no injection at all.",
		WhyItMatters:      "A new dalfox letter will arrive with a version bump and will be stored and rendered with no reference content behind it. The safe default is to make the operator read the evidence string and the detection_method rather than the badge, because the badge is the one field dalfox has told us it intends to change.",
		FalsePositive:     "A high-looking badge on a row that has no raw_request and no browser step behind it. Also an I row read as an XSS: an outdated jQuery or AngularJS build is a component-inventory fact, dalfox says so in its own description, and it still renders in the same list as V and A. And any row with detection_method library carrying dalfox's default severity while containing no reproducible request, or an oob row reported against a specific parameter when the collaborator host was shared across every vector in the run.",
		SeverityNote:      "Store dalfox's severity, act on it only after a browser confirms execution or an oob callback is attributed to one vector. For an unrecognised letter, hold at the lower of dalfox's severity and medium until the detection_method is understood. I findings are informational by dalfox's own description and should never be filed as XSS.",
		ValidationSteps: []string{
			"Read detection_method FIRST, not kind. It is the stable axis. reflection means a payload was found in a response. dom-verification means a position check on a parsed response. ast means static source-to-sink with no payload sent. oob means an out-of-band callback fired. library means a version fingerprint.",
			"Read the parenthesised suffix at the end of evidence. For dom-verification it names one of five position classes: the dalfox marker matched by CSS selector, an executable scheme (javascript: or data:text/html) in a dangerous attribute, an injected element carrying a sink-calling handler, a sink call inside <script> whose AST range covers the payload, and an inline-handler breakout. For reflection it names how dalfox had to treat the response to find its payload; anything mentioning decoding means the application encoded your characters. Read the words, do not match a remembered list, because the exact strings move between versions.",
			"Check whether raw_request and raw_response are populated. Present means a request was sent and a response observed and you can replay it. Absent means the row came from static analysis or a preflight fingerprint and there is nothing to replay.",
			"If raw_request is present, replay it verbatim with curl using the stored request line, Host and headers, and confirm the response still matches what is stored.",
			"Whatever the letter, load the URL in a real browser with DevTools open before describing anything as execution. Dalfox v3 never opened one.",
			"Confirm dalfox did not abandon the vector. A run can be reported incomplete with an error code such as SESSION_LOST in the report's meta line, in which case \"no other findings\" means \"not scanned\", not \"clean\".",
		},
		References: []string{
			"https://github.com/hahwul/dalfox",
			"https://dalfox.hahwul.com/guide/detection-model/",
			"https://dalfox.hahwul.com/reference/cli/",
			"https://portswigger.net/web-security/cross-site-scripting/",
			"https://cwe.mitre.org/data/definitions/79.html",
			"https://cwe.mitre.org/data/definitions/1104.html",
			"https://owasp.org/www-community/attacks/xss/",
		},
	},
	"dalfox|A": {
		Title:             "Dalfox A: a static source-to-sink path in JavaScript dalfox parsed. No payload was sent.",
		WhatItProved:      "Dalfox fetched the page, extracted its inline <script> blocks, built an AST, and found a data flow from a named DOM source to a named dangerous sink. The evidence string is the entire finding and has the shape \"URL:LINE:COL - DOM-based XSS via SOURCE to SINK (Source: ..., Sink: ...)\". A reference row reads \"https://ginandjuice.shop/blog/?back=%2Fblog%2F&search=rs0n:7:29 - DOM-based XSS via URLSearchParams.get(search) to document.write (Source: URLSearchParams.get(search), Sink: document.write)\", and that path is checkable by eye: the blog page loads /resources/js/deparam.js and /resources/js/searchLogger.js, and the search value reaches a document.write of an <img src=...> string. Dalfox's source and sink catalogues are large and specific (sinks include document.write, innerHTML, outerHTML, insertAdjacentHTML, createContextualFragment, setTimeout, setInterval, Function, location.href/assign, srcdoc, setAttribute, DOMParser.parseFromString, setHTMLUnsafe; sources include URLSearchParams.get, location.hash/search/pathname, document.referrer, document.cookie, window.name, postMessage event.data, localStorage.getItem, XHR responseText), so the labels mean what they say.",
		WhatItDidNotProve: "No payload was sent and nothing was observed coming back. This is static analysis of code, not a sent-and-observed result, which is why an A finding has NO raw_request and NO raw_response even though the framework always passes --include-all. The payload field on an A row, for example \"search=<img src=x onerror=alert(1) class=dlx65461efe>\", is a proof-of-concept dalfox constructed from the sink type. It was never delivered and never confirmed. Dalfox's own type_description for A is transitional wording, a method label rather than an exploitability claim, and dalfox's tooling guidance tells integrators to select AST findings by detection_method == \"ast\" rather than by type == \"A\" because the letter is being folded into the confidence axis. Static taint cannot see a sanitiser inside a function it did not inline, cannot see DOMPurify called on a later tick, cannot tell whether the guarded branch is ever entered, and does not know whether the sink is reachable with values the application accepts. The stored insertion_point does not describe an A finding either: AST analysis runs over the response body and is independent of where the scan was aimed, so a vector aimed at a cookie produces A rows naming a query-string source. Reference rows exist stored with insertion_point \"cookie\" while naming URLSearchParams.get(search). The source named in the evidence is authoritative; the insertion point is only the vector that happened to trigger the fetch.",
		WhyItMatters:      "DOM XSS is invisible to every server-side reflection check, so this is the only detector in the XSS section that finds it at all without a browser. When it is right it is a first-class CWE-79 that no proxy log would have shown. When it is wrong it costs almost nothing to find out, because the check is reading a dozen lines of JavaScript.",
		FalsePositive:     "The named source and sink both exist but no attacker string travels between them at runtime. Common shapes: the flow passes through a sanitiser in a helper dalfox did not inline; the branch guarding the sink is entered only for values the server never returns; the sink is location.assign and the code prefixes a fixed origin so only a path is controllable; or the script is dead code shipped by a framework. There is a second, quieter failure that is not a false positive but inflates the count the same way: DUPLICATES. A reference scan holds eight A rows that all describe the same script location, the same source and the same sink, differing only by which target URL was loaded and by dalfox's random dlx class token. That is one defect, not eight. Dedupe on (script location, source, sink) before you count or report.",
		SeverityNote:      "Dalfox rates A medium and the framework stores dalfox's severity and confidence unchanged; confidence is only ever high or low, and a high there means high confidence in the code path, not in exploitability. Medium is the right holding value. Promote to high only after the browser step shows execution. Do not report an A finding on its own. A source-to-sink listing without a working URL is a code-review note, not a vulnerability report.",
		ValidationSteps: []string{
			"Read the evidence and write down three things separately: the SOURCE, the SINK, and the URL dalfox was looking at. Ignore the payload field for now, it was never sent.",
			"Fetch that page and find the script yourself: curl -s \"URL_FROM_EVIDENCE\" -o page.html ; grep -n \"SINK_NAME\" page.html . If the sink is not in the HTML, the script is external: read the <script src> list and fetch each file, then grep those. On the reference the flow lives in /resources/js/searchLogger.js and /resources/js/deparam.js, not in the page body.",
			"Do not treat the :LINE:COL suffix as a document offset. Dalfox numbers within the script text it parsed, so the line frequently will not match the file. Locate the code by NAME, then read the lines around it.",
			"Trace the flow by hand: confirm the source value reaches the sink with no encoding call between them. Dalfox knows about encodeURIComponent, encodeURI, DOMPurify.sanitize, escapeHtml, htmlEncode, he.encode and createTextNode. If one of those sits on the path and dalfox still reported, the finding is wrong.",
			"Confirm the source is attacker-controlled at that URL. URLSearchParams.get('search') means the query string. location.hash means the fragment and never reaches the server. document.referrer and window.name need a hop from a page you control.",
			"Build a payload for THE SINK, not from the payload field. A document.write of '<img src=\"...'+q+'...\">' needs you to close the attribute and the tag, so \"><img src=x onerror=alert(1)> is the shape, not a bare <img>. An innerHTML sink needs a non-script element carrying a handler. A setTimeout or Function sink needs a bare expression.",
			"Load the crafted URL in a real browser with DevTools open and confirm the alert. This step is mandatory: nothing in an A finding involved a browser.",
			"Negative control: load the same URL with the source parameter set to a harmless marker, confirm nothing fires, and confirm your marker appears at the sink. That separates \"the sink is live\" from \"the page has an alert somewhere\".",
		},
		References: []string{
			"https://github.com/hahwul/dalfox",
			"https://dalfox.hahwul.com/guide/detection-model/",
			"https://dalfox.hahwul.com/reference/cli/",
			"https://portswigger.net/web-security/cross-site-scripting/dom-based",
			"https://portswigger.net/web-security/cross-site-scripting/dom-based/lab-document-write-sink",
			"https://portswigger.net/web-security/cross-site-scripting/cheat-sheet",
			"https://cwe.mitre.org/data/definitions/79.html",
		},
	},
	"dalfox|R": {
		Title:             "Dalfox R: the payload came back in the response, position not confirmed exploitable",
		WhatItProved:      "Dalfox sent a payload and found it in the response body. That is the whole claim, and dalfox states it plainly in its own type_description: \"The payload came back in the response, but its position was not confirmed exploitable.\" The most useful part of the row is the parenthesised qualifier at the end of the evidence, which is easy to skip: it names how dalfox had to treat the response before it could find its own payload. Any qualifier mentioning decoding of any kind means the application encoded your characters and dalfox decoded them to match. Related, inject_type sometimes carries a shape label such as inHTML-HPP for a duplicate-parameter echo. Because the framework passes --include-all, R rows carry raw_request and raw_response, so the reflection is re-checkable from stored bytes.",
		WhatItDidNotProve: "It did not establish position, and dalfox says so in the string. Dalfox ran its DOM verification against this input and it did NOT succeed; had it succeeded the row would be a V. So an R is closer to a negative result than a positive one: the payload reflected and the executable-position check failed. A decoding qualifier is worse still, because it means the response contained < and > where the payload had < and >, which is the application's encoder working correctly reported as a finding. Note also that dalfox's R payloads are frequently scheme payloads such as javascript:alert(1), which mean nothing unless the value lands in an href, src, action, formaction or a location assignment. A javascript: string reflected into page text is inert. Do not read the exact qualifier wording as a fixed vocabulary; it moves between versions, so read what it says rather than matching a remembered list.",
		WhyItMatters:      "R is stored at severity info with confidence low for a reason, and the honest default is to leave it there. It earns attention in two situations only: the qualifier reports a plain reflection with no decoding AND the payload was a scheme payload AND the landing position is a URL-bearing attribute; or the same parameter also carries a V or an A, in which case the R is corroborating detail rather than a finding of its own.",
		FalsePositive:     "By dalfox's own definition an R does not assert exploitability, so most R rows are not false positives so much as non-findings. The ones that waste the most time: a scheme payload such as javascript:alert(1) reflected into ordinary page text or into a JS string literal, where the scheme means nothing. On the reference target, category=javascript:alert(1) on /catalog lands inside const selectedCategory = \"...\"; , a double-quoted JS string where a javascript: prefix does exactly nothing (verified live: the value comes back raw there and entity-encoded in the hidden input above it). Then: a reflection that only exists after decoding, which is proof the escaping works; and a reflection into a JSON response served with a Content-Type that never renders.",
		SeverityNote:      "Info, and it should stay info. Promoting an R because the payload looks dangerous is the most common overclaim in this section. If validation produces execution, the finding is no longer an R and should be rewritten as reflected XSS at the severity the position and session justify. If validation does not, it is not reportable.",
		ValidationSteps: []string{
			"Read the qualifier in parentheses at the end of the evidence FIRST. If it mentions decoding of any kind, the application encoded your characters and you can usually stop there.",
			"Replace the payload with a marker that cannot be confused with page content and re-send: curl -sG \"https://TARGET/PATH\" --data-urlencode \"PARAM=rs0nMARKER\" | grep -n rs0nMARKER . Every hit is a distinct reflection with its own context.",
			"For each hit, name the context out loud: HTML text node, quoted attribute value, unquoted attribute, quoted JS string, unquoted JS token, URL-bearing attribute, HTML comment, or JSON body.",
			"Send the specific characters that context needs, one request, url-encoded, and grep for them RAW. HTML text needs < and >. A quoted attribute needs its quote. A quoted JS string needs that quote and </script. A URL attribute needs an executable scheme prefix to survive. If they come back entity-encoded the reflection is inert.",
			"Only if the previous step shows the required characters surviving raw, build a context-appropriate payload and load it in a browser with DevTools open.",
			"Check the response Content-Type: curl -sI \"https://TARGET/PATH?PARAM=x\" | grep -i content-type . A reflection inside application/json that no browser renders as HTML is not XSS, and dalfox does not gate on this.",
		},
		References: []string{
			"https://github.com/hahwul/dalfox",
			"https://dalfox.hahwul.com/guide/detection-model/",
			"https://dalfox.hahwul.com/reference/cli/",
			"https://portswigger.net/web-security/cross-site-scripting/reflected",
			"https://portswigger.net/web-security/cross-site-scripting/contexts",
			"https://cwe.mitre.org/data/definitions/79.html",
			"https://owasp.org/www-community/attacks/xss/",
		},
	},
	"dalfox|V": {
		Title:             "Dalfox V: dalfox asserts this input is exploitable. Read detection_method to learn whether anything ran.",
		WhatItProved:      "Dalfox issues V under two different detection methods and they are not the same evidence, so read detection_method before reading the badge. With detection_method \"dom-verification\" (the normal case), dalfox sent the payload, received a response, parsed that response, and located the payload in a position where script would run if a browser rendered it. The parenthesised suffix on the evidence names which position class dalfox decided on. Dalfox 3.2.1 documents five: the dalfox marker matched by CSS selector; an executable scheme (javascript: or data:text/html) in a dangerous attribute; an injected element carrying a sink-calling handler; a sink call inside <script> whose AST range covers the payload; and an inline-handler breakout. With detection_method \"oob\", a blind payload reached an out-of-band collector, which is dalfox's only execution evidence and is off unless the operator set -b or --blind-oob. Dalfox's own type_description for V is \"Dalfox asserts this input is exploitable. Act on it.\" Because the framework always passes --include-all, a V ships with raw_request and raw_response, so the exact bytes behind the assertion are stored and replayable. On the reference target dalfox is right about the position: GET /catalog?searchTerm=X places X raw inside a single-quoted JS string literal, verified live as \"var searchText = 'rs0nMARKER';\", so a payload that closes the literal and appends a statement lands in executable position.",
		WhatItDidNotProve: "For a dom-verification V, nothing executed. Dalfox v3 is a Rust rewrite (the Go code is frozen on the v2 branch) and ships NO headless browser, so \"DOM verification\" is an AST and DOM parse of response text, not a page load. Dalfox's own presentation guidance says it outright: avoid describing findings as executed, and reserve that word for detection_method \"oob\". A static position judgement cannot see a Content-Security-Policy that refuses the injected inline handler, cannot see a runtime sanitiser applied after render, cannot see a framework that re-escapes on hydration, and cannot tell whether the code path is ever reached. Note that confidence is only ever \"high\" or \"low\" and confidence_reason names the deciding signals, so a CSP concern shows up as a reason string and does not by itself downgrade the type. For an oob V the gap is different: a callback proves the payload executed somewhere, but not which request triggered it, not which user, not which origin, and not that it is reachable by the delivery route you intend to report. Neither method proves impact: alert(1) in a JS string literal demonstrates script control, not that anything valuable is reachable from that origin.",
		WhyItMatters:      "V is the strongest thing dalfox emits and the one an operator will act on without checking. It is correct often enough to be trusted and static often enough to be wrong, so a dom-verification V needs exactly one browser load to become a real finding. Once converted it is CWE-79 with genuine impact against a same-origin session, and the raw request stored alongside it makes the write-up trivial.",
		FalsePositive:     "The payload is present and dalfox's position class is plausible, but the browser never runs it. Three recurring shapes. First, CSP: the page returns script-src 'self', the injected handler is refused, and the console shows a violation while the row still says V. Second, a position class that is real but inert: an executable scheme in an attribute the page never navigates, or an injected element inside a <template> or <noscript> that is never adopted into the live document. Third, the JS-context case where the value sits in a string literal but the server escapes the quote, so the breakout arrives entity-encoded and closes nothing. In all three the alert never appears at the browser step, and what you have is a reflection with a good story. For an oob V the equivalent failure is attribution: the callback fired from a different injection point, a different page, or a stored payload planted by an earlier scan.",
		SeverityNote:      "Store dalfox's severity, report as high only after a browser confirms execution. Before that a dom-verification V is at most medium: it is a well-evidenced assertion about byte position, not a demonstrated vulnerability. An oob V is genuinely execution and can be high on the technical fact, but must carry an explicit note that the delivery route is unattributed until you have re-tested with a unique collaborator host per vector. After execution is confirmed, severity follows what the origin holds. Reflected XSS requiring a crafted link is typically medium to high; the same execution inside an authenticated session is high.",
		ValidationSteps: []string{
			"Read detection_method first. \"dom-verification\" means a parsed response and no browser. \"oob\" means an out-of-band callback fired and the payload ran somewhere. \"reflection\" on a V row means the position judgement came from body matching alone and deserves the R-level scepticism.",
			"Open the finding's Raw Request panel and copy the request line and Host header. That is the exact request dalfox sent, not a reconstruction.",
			"Reproduce with curl and look at WHERE the value landed, not whether it is present: curl -sG \"https://TARGET/PATH\" --data-urlencode \"PARAM=rs0nMARKER\" | grep -n rs0nMARKER . On the reference, curl -sG \"https://ginandjuice.shop/catalog\" --data-urlencode \"searchTerm=rs0nMARKER\" returns the line \"var searchText = 'rs0nMARKER';\" (line number moves as the page changes, so grep, do not count lines).",
			"Classify that position yourself. Inside a quoted JS string you need the matching quote or </script. Inside HTML text you need <. Inside a quoted attribute you need the matching quote. Inside href/src/action you need an executable scheme such as javascript: or data:text/html. Confirm your classification agrees with the suffix dalfox put in the evidence.",
			"Send only the characters that class requires, url-encoded, in one request, and grep the response for them RAW. If they come back as ' or \" or < the position is not reachable and the V is wrong.",
			"Do the thing dalfox did not: paste the finding's URL into a real browser with DevTools open. A dom-verification V is confirmed only when the alert box appears, or when you can see your injected statement in the Sources panel as parsed JavaScript.",
			"Negative control: load the same URL with the payload replaced by a harmless string of the same length. If the alert still fires, something else on the page is firing it and the finding is not yours.",
			"Check for CSP: curl -sI \"https://TARGET/PATH?PARAM=x\" | grep -i content-security-policy . A script-src without 'unsafe-inline' kills an inline-handler payload even though dalfox reported V, and the browser console will say so explicitly.",
			"For an oob V, do not skip to a report. The callback tells you the payload ran, not where. Re-send the same payload with a uniquely identifying collaborator subdomain per insertion point, and only claim the insertion point whose unique host called back.",
		},
		References: []string{
			"https://github.com/hahwul/dalfox",
			"https://dalfox.hahwul.com/guide/detection-model/",
			"https://dalfox.hahwul.com/reference/cli/",
			"https://portswigger.net/web-security/cross-site-scripting/reflected",
			"https://portswigger.net/web-security/cross-site-scripting/contexts",
			"https://portswigger.net/web-security/cross-site-scripting/cheat-sheet",
			"https://cwe.mitre.org/data/definitions/79.html",
			"https://owasp.org/www-community/attacks/xss/",
		},
	},
	"domdig|*": {
		Title:             "domdig, kind not yet catalogued: it ran in Chromium, so ask what the browser actually observed",
		WhatItProved:      "domdig runs a real puppeteer-managed Chrome. The container deliberately lets puppeteer download its own exact browser build, because htcrawl calls puppeteer.launch() with no executablePath and any other chromium on the box is silently ignored. So every domdig row describes something that happened during a live page load, not a text match on a response body. Its consts.js declares five types, VULNTYPE_DOM \"domxss\", VULNTYPE_POTENTIALDOM \"potentialdom\", VULNTYPE_STORED \"stored\", VULNTYPE_TEMPLATEINJ \"templateinj\" and VULNTYPE_WARNING \"warning\", but only domxss, stored and templateinj are emitted by the current code. For domxss and stored the proof is strong and specific: domdig exposes window.___xssSink into the page, every one of its XSS payloads calls window.___xssSink({0}) in place of alert(1), and the finding is created FROM that exposed function being invoked. Script the tool injected actually ran. stored means the same sink fired on a later crawl of the page, from a payload planted during an earlier one.",
		WhatItDidNotProve: "Three storage facts will mislead if not stated. First, the framework's parser hardcodes severity \"high\" for EVERY domdig kind. That is our floor, not domdig's judgement, and it will be applied unchanged to any new kind. Second, confidence is only meaningful for domxss: domdig computes confirmed as \"the current page URL contains the payload\", and our parser stores \"confirmed\" when domdig says so and \"executed, route not established\" when domdig explicitly says confirmed:false. For every other type domdig omits the confirmed argument entirely and the Vulnerability constructor's !(confirmed === false) defaults it to true, so \"confirmed\" carries no information there. Third, param is domdig's element field, which in DOM-scan mode is a CSS selector for the input it filled but in fuzz mode is the injection CHANNEL: \"GET/\" + parameterName, or \"hash\". A hash finding is fragment-only, never reaches the server, and needs a victim to follow a link. And nothing in any domdig row establishes impact: the sink firing in Chromium proves script control, and what that control is worth depends on the origin and the session, which domdig never had.",
		WhyItMatters:      "domdig is the only tool in this section whose findings involved a browser at all. When it says the sink fired, script ran. That makes it the tool most worth escalating and simultaneously the tool where an unexamined high badge will be believed without question.",
		FalsePositive:     "domdig's sink-based kinds are hard to fake, so the failure mode is almost never \"it did not happen\". It is the surrounding claim. Recurring shapes: a hash-channel finding written up without saying the payload never reaches the server; a row whose confidence reads \"executed, route not established\" reported as a working link; a DOM-scan finding that requires a form fill and an event dispatch reported as a one-click URL; and any non-domxss kind whose \"confirmed\" was taken as a verification result rather than a constructor default. Watch the other direction too: if puppeteer cannot resolve its browser build, the scan exits cleanly and reports nothing, so an absence of domdig findings is not evidence of absence.",
		SeverityNote:      "High is right for a confirmed domxss or stored, because script executed in a browser and the delivery route is established. A domxss stored as \"executed, route not established\" is high on the technical fact and must carry an explicit note that delivery is unproven. For any kind that is not sink-based, including templateinj, treat the stored high as a placeholder and hold at medium until you demonstrate execution yourself. A brand new kind arriving at severity high was given that severity by our parser, not by domdig.",
		ValidationSteps: []string{
			"Read param and decode the channel. \"GET/name\" is the query parameter name. \"hash\" is the URL fragment. Anything else is a CSS selector for a form field domdig filled during the DOM crawl, which means the finding needs that interaction to reproduce.",
			"Rebuild the URL. domdig stores one for domxss but not for templateinj, so for some kinds you must reconstruct it from the vector's base URL plus the channel from the previous step.",
			"Check the payload before sending it. domdig rewrites its sink call to alert(1) on output, so the stored payload is usually browser-ready, but confirm no leftover {0} or bare ___xssSink name remains; if one does, replace it with alert(1) yourself.",
			"Load the URL in a real browser with DevTools open. For a sink-based kind the alert should appear. Watch the console and the Network tab as it loads.",
			"If it does not fire on load, reproduce the interaction. A DOM-scan finding may need the named element filled and an input event dispatched, and domdig also dispatches synthetic events at handler-bearing elements, which a human will not reproduce by accident.",
			"Negative control: load the same URL with the payload replaced by an inert string of the same shape and confirm nothing fires.",
			"For a hash channel, remember the server never sees the fragment. Confirm the vulnerable script runs on the landing page with no server-side cooperation, and state in the write-up that delivery requires the victim to open your link.",
			"Read the row's confidence string literally. \"executed, route not established\" is our wording for domdig's confirmed:false and means the code ran but the URL did not carry the payload, so there is no one-click link yet.",
		},
		References: []string{
			"https://github.com/fcavallarin/domdig",
			"https://github.com/fcavallarin/htcrawl",
			"https://portswigger.net/web-security/cross-site-scripting/dom-based",
			"https://portswigger.net/web-security/cross-site-scripting/stored",
			"https://cwe.mitre.org/data/definitions/79.html",
		},
	},
	"domdig|templateinj": {
		Title:             "domdig templateinj: a client-side template engine evaluated an injected expression, in a real browser",
		WhatItProved:      "This one actually happened in Chromium. domdig drives a genuine puppeteer-managed Chrome, loads the mutated URL, lets the page render and events fire, then reads page().content(), the LIVE serialized DOM after JavaScript has run, and matches the regex /\\[object [A-Za-z]+\\]([0-9]+)\\[object [A-Za-z]+\\]/gm. The three payloads are {{this+{0}+this}}, {this+{0}+this} and bare this+{0}+this, where {0} is replaced by a per-payload integer key. A match means a client-side template engine on the page took the injected text, evaluated \"this + N + this\" as a JavaScript expression, stringified this to [object Something], and wrote the concatenated result into the rendered DOM. That is arbitrary expression evaluation inside the page's template context, observed rather than inferred, and it is the strongest evidence any tool in this section produces.",
		WhatItDidNotProve: "It did not prove script execution, and it did not use domdig's sink. domdig exposes window.___xssSink into the page and every one of its XSS payloads calls it in place of alert(1). The template-injection payloads DO NOT contain a sink call at all, so nothing about this row involved ___xssSink firing. Expression evaluation is a primitive; whether it becomes XSS depends entirely on the engine. AngularJS 1.2 to 1.5 sandboxed expressions and needs a published sandbox escape for the exact minor version. AngularJS 1.6 and later removed the sandbox, so {{constructor.constructor('alert(1)')()}} is direct execution. Vue 2 has no sandbox. Some template layers only interpolate values and cannot reach a constructor at all. Three storage facts also do not mean what they look like. The stored confidence \"confirmed\" is meaningless for this kind: domdig computes confirmed only for its DOM XSS type (where it checks whether the current page URL contains the payload), and its template-injection call site is utils.addVulnerability(jar, db, VULNTYPE_TEMPLATEINJ, vuln, null, null, VERBOSE) with the confirmed argument omitted, so the Vulnerability constructor's default this.confirmed = !(confirmed === false) makes every templateinj row confirmed regardless of anything; our parser then stores \"confirmed\" because domdig reported it. The row has no URL because domdig passes null for url at that call site, so the empty field is domdig's behaviour and not data lost on our side. And param is not a DOM element: in fuzz mode domdig labels the injection channel \"GET/\" + parameterName, or \"hash\". \"GET/redirect\" means the query parameter literally named redirect, not a redirect.",
		WhyItMatters:      "Client-side template injection is invisible to reflection scanners because the interesting event happens after render, and invisible to dalfox's AST because the expression sits in HTML rather than in a script block. On an unsandboxed engine it is straightforwardly CWE-79 with full script control in the origin. On the reference target the login page ships <body ng-app> with /resources/js/angular_1-7-7.js loaded (verified live), which is post-sandbox-removal, so the gap between primitive and execution is one payload.",
		FalsePositive:     "Two shapes. The regex shape: [object Something]<digits>[object Something] is distinctive but it is still a substring match over serialized DOM, so a page that renders attacker text into a debug dump, or that legitimately stringifies objects around a number matching domdig's key, will match with no template engine involved. Loading it in a browser kills this instantly, because a non-evaluating page shows {{7*7}} verbatim. The overclaim shape is more common and more damaging: a genuine templateinj where the browser shows 49 but the constructor payload never runs, written up as XSS anyway. A sandboxed engine, or a layer that interpolates values but cannot resolve a constructor, gives you 49 and never gives you alert(1).",
		SeverityNote:      "The framework stores every domdig kind as high because the parser hardcodes it, not because domdig judged it. High is correct once the constructor payload executes. Until then this is a confirmed primitive with no demonstrated impact and should be held at medium, on the same principle that prototype pollution without a gadget is not RCE. Never carry the stored \"confirmed\" into a report as though it were a verification result; for this kind it is a constructor default.",
		ValidationSteps: []string{
			"Recover the URL, because the row does not carry one. Combine the vector's base URL with the parameter name inside the param field: \"GET/redirect\" on https://ginandjuice.shop/login means https://ginandjuice.shop/login?redirect= .",
			"Confirm the braces survive the server, which curl CAN show: curl -sG \"https://TARGET/PATH\" --data-urlencode \"PARAM={{7*7}}\" | grep -n \"PARAM\" . On the reference this returns a hidden input whose value is {{7*7}} with the braces intact.",
			"Understand that the previous step is the limit of curl. Evaluation happens in the browser, so a raw {{7*7}} in the curl output is EXPECTED and proves nothing on its own.",
			"Identify the engine before crafting anything: curl -s \"https://TARGET/PATH\" | grep -niE \"angular|vue|ng-app|v-model|mavo|handlebars\" . On the reference: angular_1-7-7.js and <body ng-app>.",
			"Load the braces URL in a real browser and inspect the element. If the value now reads 49 instead of {{7*7}}, the engine evaluated your expression and the primitive is confirmed exactly as domdig saw it.",
			"Escalate to execution with an engine-appropriate payload. AngularJS 1.6+ and Vue 2: {{constructor.constructor('alert(1)')()}} . AngularJS 1.2 to 1.5: you need the published sandbox escape for that exact minor version, and if you cannot find one the finding stops at the primitive.",
			"Negative control: request the same URL with PARAM=7*7 and no braces, and confirm the page shows the literal 7*7. If it shows 49, something else is evaluating and your interpolation is not the cause.",
		},
		References: []string{
			"https://github.com/fcavallarin/domdig",
			"https://github.com/fcavallarin/htcrawl",
			"https://portswigger.net/research/xss-without-html-client-side-template-injection-with-angularjs",
			"http://blog.angularjs.org/2016/09/angular-16-expression-sandbox-removal.html",
			"https://sites.google.com/site/bughunteruniversity/nonvuln/angularjs-expression-sandbox-bypass",
			"https://portswigger.net/web-security/cross-site-scripting/dom-based",
			"https://cwe.mitre.org/data/definitions/79.html",
		},
	},
	"forbidden|*": {
		Title:             "Access control bypass candidate: a status code changed, which is not the same as access",
		WhatItProved:      "Forbidden re-sent a request differing from the refused one by a header, a path mutation or a verb, and got a different status, usually 200, whose length did not match the denial page.",
		WhatItDidNotProve: "That the protected resource came back. Its only content baseline is the ORIGINAL DENIAL: it never compares the response against the ordinary, no-header response of the URL it actually requested. So an allowed page returned under a header that did nothing looks exactly like a real bypass.",
		WhyItMatters:      "Where it is real, a caller reaches something the application meant to refuse. This class is almost always a split between where the check is applied, the request path, and where the routing decision is made, the header.",
		FalsePositive:     "The common case, so expect it. The tool requested a DIFFERENT, allowed URL with an extra header that changed nothing and got that allowed page back; it survived only because the length differs from the denial page. On ginandjuice.shop two of the three reported bypasses were exactly this.",
		SeverityNote:      "Medium as reported, because it is a candidate. High or critical only once you can name a string in the body that the denial withheld.",
		ValidationSteps: []string{
			"Fetch the protected path plainly and confirm it is still refused. Note the status and length.",
			"NEGATIVE CONTROL, the step that kills most findings in this family: fetch the finding's own URL with NO extra header.",
			"Run the finding's request, the curl above.",
			"Compare the result against the CONTROL first, before comparing it against the denial. Identical bodies mean the header did nothing and the finding is dead.",
			"If it survives, name the privileged string: point at something in the body the denial withheld, such as a username or an admin control. Byte length alone is not enough, since pages vary on their own.",
		},
		References: []string{
			"https://portswigger.net/web-security/access-control",
			"https://cwe.mitre.org/data/definitions/284.html",
		},
	},
	"forbidden|access-control-bypass": {
		Title:             "Access control bypass candidate: a status code changed, which is not the same as access",
		WhatItProved:      "Forbidden re-sent a request differing from the refused one by a header, a path mutation or a verb, and got a different status, usually 200, whose length did not match the denial page.",
		WhatItDidNotProve: "That the protected resource came back. Its only content baseline is the ORIGINAL DENIAL: it never compares the response against the ordinary, no-header response of the URL it actually requested. So an allowed page returned under a header that did nothing looks exactly like a real bypass.",
		WhyItMatters:      "Where it is real, a caller reaches something the application meant to refuse. This class is almost always a split between where the check is applied, the request path, and where the routing decision is made, the header.",
		FalsePositive:     "The common case, so expect it. The tool requested a DIFFERENT, allowed URL with an extra header that changed nothing and got that allowed page back; it survived only because the length differs from the denial page. On ginandjuice.shop two of the three reported bypasses were exactly this.",
		SeverityNote:      "Medium as reported, because it is a candidate. High or critical only once you can name a string in the body that the denial withheld.",
		ValidationSteps: []string{
			"Fetch the protected path plainly and confirm it is still refused. Note the status and length.",
			"NEGATIVE CONTROL, the step that kills most findings in this family: fetch the finding's own URL with NO extra header.",
			"Run the finding's request, the curl above.",
			"Compare the result against the CONTROL first, before comparing it against the denial. Identical bodies mean the header did nothing and the finding is dead.",
			"If it survives, name the privileged string: point at something in the body the denial withheld, such as a username or an admin control. Byte length alone is not enough, since pages vary on their own.",
		},
		References: []string{
			"https://portswigger.net/web-security/access-control",
			"https://cwe.mitre.org/data/definitions/284.html",
		},
	},
	"ghauri|*": {
		Title:             "Ghauri finding: how to read any of them",
		WhatItProved:      "Ghauri confirmed one technique against one parameter, using a genuine differential rather than a single suspicious response. For boolean-based it injects matched TRUE/FALSE condition pairs (its own examples are of the form 2*3*8=6*8 against 2*3*8=6*9) and compares HTTP status code, response content and content length. For time-based it injects DBMS-specific sleep functions and varies the sleep duration, checking the measured time follows. Findings stored here were parsed out of Ghauri's stdout results block, the block that begins at \"Parameter:\" and carries Type, Title and Payload lines, which is the only structured output the tool produces.",
		WhatItDidNotProve: "Ghauri has no machine-readable report, so the evidence is scraped text and anything it printed outside that block was not captured. Whether data was extracted depends on the run's settings: the framework adds no enumeration flags of its own, but --banner, --current-db, --current-user, --dbs, --tables, --columns and --dump are operator-settable, so check before writing \"nothing was read\". The param field carries Ghauri's own label including the place, for example \"category (GET)\", rather than a bare parameter name, so it is not directly pasteable into a request. As with sqlmap, a finding from a run that carried a session cookie or auth header does not establish pre-auth reachability.",
		WhyItMatters:      "Ghauri is the independent second opinion in this section. Agreement between Ghauri and sqlmap on the same parameter and the same technique is the strongest signal available before you validate by hand, and it is worth stating in a report. Disagreement is informative rather than alarming, because the two implement different technique sets: Ghauri's own --technique default is BEST, meaning Boolean, Error, Stacked and Time, so it will never report UNION query or inline query, and its silence on those means nothing at all.",
		FalsePositive:     "The same failure modes as sqlmap: an unstable page defeats the content comparison, and a slow or rate-limited target defeats the timing comparison. Ghauri-specific: a replayed session verdict, if it was run by hand without --flush-session. Symptom: the result arrives implausibly fast, the reported HTTP request count is very low, or every vector on a host returns an identical verdict. Also watch for a finding attributed to a different parameter than the one the framework's vector claimed. Symptom: the param label does not match the vector's parameter list.",
		SeverityNote:      "Severity is assigned by technique using the same scale as sqlmap: stacked queries critical; error-based and boolean-based blind high; time-based blind medium. Ghauri's confidence string reads \"confirmed by ghauri\", which means its own confirmation round passed, not that a human has reproduced it.",
		ValidationSteps: []string{
			"Go to the technique entry matching the kind badge. Ghauri's technique names match sqlmap's for the four techniques it implements, and so do the validation procedures.",
			"Strip the place suffix from the parameter before building a request by hand. \"category (GET)\" means parameter category in the query string; the parenthesised part is Ghauri's label, not part of the name.",
			"Establish a control request and reproduce by hand, as with sqlmap. The tool's word is a starting point, never the evidence you submit.",
			"Check whether sqlmap reported the same technique on the same parameter. If it did, put that in the report; two independent implementations agreeing on the same differential is worth writing down explicitly.",
			"If Ghauri reported a finding and sqlmap reported nothing for the same vector, suspect the run before you suspect Ghauri. Confirm the sqlmap command actually tested that insertion point: a cookie or a custom header is only tested when its value carries the literal * marker, and at default level a plain --cookie tests nothing and exits 0.",
			"Treat a CLEAN Ghauri result as meaningful only if --flush-session was in the command. Ghauri keys its session by HOST rather than by URL and parameter, so without that flag it replays the first verdict it ever formed for that host without sending a request. This framework always sets it; if you are reproducing by hand, set it yourself.",
		},
		References: []string{
			"https://github.com/r0oth3x49/ghauri",
			"https://deepwiki.com/r0oth3x49/ghauri/4-sql-injection-detection",
			"https://portswigger.net/web-security/sql-injection",
			"https://cwe.mitre.org/data/definitions/89.html",
		},
	},
	"ghauri|boolean-based blind": {
		Title:             "Boolean-based blind SQL injection, confirmed by Ghauri's true/false pair comparison",
		WhatItProved:      "Ghauri injected matched pairs of conditions that differ only in truth value and observed the two responses diverge in status code, content, or content length. The stored payload is one arm of that pair. Two distinct vector families show up here and they are proved differently. A WHERE or HAVING clause vector such as category=Accessories' AND 09853=9853-- wXyW appends a condition to a legitimate value; its false twin is one digit away (09853=9854) and the discriminator is the rendered result set. A \"Parameter replace\" vector such as category=rs0n' AND 0546=(SELECT (CASE WHEN (02010=2010) THEN 03586 ELSE 3*(SELECT 2 UNION ALL SELECT 1) END))-- works differently: rs0n is this framework's canary value substituted because the vector carried no observed value, and the CASE construct makes the FALSE branch raise a database error, because a subquery returning more than one row is used in a scalar context. For that family the discriminator is error versus no error, often 500 versus 200, not a difference in the product list. Either way, the divergence proves the backend parsed and evaluated the injected condition.",
		WhatItDidNotProve: "No data was returned. This is a one-bit-per-request oracle and nothing was asked of it. Nothing was established about tables, columns, row counts or privileges. The stored payload is a single arm, so sending it alone reproduces nothing: for the WHERE/HAVING family it renders a normal-looking page, and for the Parameter replace family it renders the empty page for the canary value. It also does not prove reachability without whatever cookies the scan carried, and for a Parameter replace vector it does not prove the parameter behaves the same way with its real production value.",
		WhyItMatters:      "A confirmed boolean oracle is full read access to everything the database user can SELECT, at roughly seven to eight requests per character with a binary search. The confidentiality impact matches a UNION finding; only the cost differs. Ghauri reaching this independently of sqlmap on the same parameter is the strongest pre-manual signal this section produces.",
		FalsePositive:     "A page with per-request variable content, so any two responses differ. Symptom: the stability control returns two different byte counts. Second mode: the application reflecting the parameter value into the body, so the two arms differ by exactly the digits you changed and nothing else. Symptom: diff the bodies and your own payload text is the only delta. Third mode, specific to Parameter replace vectors: an application that returns a generic 500 for any malformed input, so the error branch is indistinguishable from ordinary breakage. Symptom: a bare single quote also gives 500, which is true on the reference target (verified: category=' returns 500 with a normal HTML error page), so the error-versus-normal split must be shown to track the inner condition rather than the presence of a quote. Fourth mode: a WAF blocking one arm and passing the other, so you are comparing a block page against a real page. Symptom: differing status codes or a vendor block message in one body.",
		SeverityNote:      "High. Do not downgrade it because nothing came back; nothing was requested. Downgrade only for genuine reachability limits, such as the parameter existing solely behind credentials the scan supplied, and say so explicitly. If the finding is Parameter replace and you cannot reproduce it against the real parameter value, hold the report until you can.",
		ValidationSteps: []string{
			"Stability control first. Send the unmodified request twice and record the byte count both times: curl -s -G \"https://ginandjuice.shop/catalog\" --data-urlencode \"category=Accessories\" -o /dev/null -w \"%{http_code} %{size_download}\\n\". Both must agree. If they disagree, do not use byte count; pick a stable marker string in the body and grep for it instead. Measure your own baseline rather than reusing a number from a write-up, since the reference site's page sizes drift.",
			"Identify which vector family you have by reading the evidence line. \"AND boolean-based blind - WHERE or HAVING clause\" means an appended condition. \"Boolean-based blind - Parameter replace\" means Ghauri substituted the value, and the payload will contain the canary rs0n rather than the real value.",
			"For a WHERE/HAVING vector: send the stored TRUE arm as-is, then derive the FALSE arm by changing one digit in the comparison (09853=9853 becomes 09853=9854) and leaving the random comment suffix (-- wXyW) and everything else byte identical. Record status and size for both.",
			"For a Parameter replace vector: send the payload as stored, which has the inner condition TRUE (02010=2010) and therefore does NOT hit the error branch. Then send it with the inner condition made false (02010=2011). The false version must raise a database error or a 500 while the true version returns a normal page. If both return the same status and the same body, this vector has no oracle and the finding is unproved.",
			"Compare and repeat. The two arms must differ from each other and each must reproduce its own result on a second send. On the reference target the hand-built equivalent pair is ' AND 1=1-- returning the base page and ' AND 1=2-- returning a page with an empty product list.",
			"Negative control for text shape. The two arms differ by exactly one character and are the same length, so payload length, quoting and comment syntax cannot account for the difference. Then confirm the app is not merely reacting to an unknown value: send category=AccessoriesZZZ with no SQL syntax. If that returns the same page as your TRUE arm, your oracle is not measuring SQL evaluation.",
			"For a Parameter replace vector, redo the pair against the parameter's REAL value rather than the canary. A finding proved only against rs0n does not show the production code path behaves the same way, and a triager will ask.",
			"Demonstrate the oracle rather than the payload: substitute an expression the database must compute and whose truth you can flip and predict, and confirm the page tracks it every time. Then extract one harmless value, such as the first character of the DBMS version, for the report.",
		},
		References: []string{
			"https://portswigger.net/web-security/sql-injection/blind",
			"https://github.com/r0oth3x49/ghauri",
			"https://deepwiki.com/r0oth3x49/ghauri/4-sql-injection-detection",
			"https://portswigger.net/web-security/sql-injection/cheat-sheet",
			"https://cwe.mitre.org/data/definitions/89.html",
		},
	},
	"ghauri|error-based": {
		Title:             "Error-based SQL injection, confirmed by Ghauri retrieving a computed value from a database error",
		WhatItProved:      "Ghauri forced the DBMS to raise an error carrying the result of an expression Ghauri chose, and the application rendered that error text in the response. Two things at once: the backend evaluated attacker-supplied SQL, and the application leaks raw database error text to clients. This technique RETURNS data, one value per request, with no column-count matching and no bit-by-bit inference. Ghauri's evidence for this is a line in its stdout results block, not a machine-readable record, so the exact error text it matched is not preserved in the finding and you have to recover it yourself.",
		WhatItDidNotProve: "Nothing about which data is reachable, unless enumeration options were enabled on the run; Ghauri asked only for its own probe value otherwise. It does not prove errors surface in other code paths or for other error classes. It says nothing about UNION (Ghauri does not implement it), nothing about stacked queries, and nothing about write access. It does not prove the error page is reachable unauthenticated if the scan carried a session.",
		WhyItMatters:      "Error-based is the cheapest extraction Ghauri has: roughly one value per request with the output visible in the response. Separately, verbose database errors reaching a client is a reportable issue in its own right and gives an attacker the DBMS, its version, and often schema and column names for free. Note the absence case: a target that returns a 500 whose body carries no database error text, as the reference target does for a bare single quote, suppresses error-based detection entirely while leaving boolean and time techniques fully viable. No error-based finding next to a confirmed boolean finding is normal.",
		FalsePositive:     "The application echoing the offending input into a generic 500 or validation page, so your marker returns because you sent it. The bare-quote control catches it. Second mode: a WAF, CDN or framework error page containing a phrase that resembles a DBMS error. Symptom: the identical page appears for any malformed value, including ones with no injection syntax. Third mode: a returned value that does not change when you change the expression. Symptom: the expression-tracking step yields the same number twice. Fourth mode, Ghauri-specific: a stale session verdict if the tool was run by hand without --flush-session, since it keys sessions by host.",
		SeverityNote:      "High. Output returns in the response body, so extraction is cheap and the proof is direct rather than inferential. The verbose error exposure is an additional finding under CWE-209. Do not read the absence of this kind as evidence that a target is not injectable.",
		ValidationSteps: []string{
			"Send the stored payload URL encoded and read the response BODY, not the status line. Locate the database error text and the value embedded in it. If the body is generic or carries no database text, this kind is not supported by this response and the finding needs re-examination.",
			"Prove the value tracks your expression and not your input. Send the payload twice with the embedded expression changed from one returning 1+1 to one returning 2+2, and confirm the value in the error changes from 2 to 4. Skipping this step is how an echo gets reported as an extraction.",
			"Negative control for echo. Send a payload that breaks the query but contains no computable expression, for example a bare single quote, and confirm the error page does NOT contain your marker. If it does, the application is reflecting your input into its error handler and no value was ever computed.",
			"Retrieve one harmless real value for impact using the DBMS-appropriate form: cast a concatenation containing (SELECT version()) to NUMERIC on PostgreSQL, EXTRACTVALUE or the double-query form on MySQL, CONVERT to int on MSSQL. Screenshot the error page containing it.",
			"Quote the exact error string verbatim in your notes, since Ghauri did not preserve it and the verbose-error exposure is separately reportable.",
			"Repeat the impact step with any session cookie removed, if the endpoint allows it, to establish whether the leak is pre-auth.",
			"Cross-check against sqlmap. If sqlmap also reported error-based on this parameter, note the agreement; if sqlmap reported UNION query as well, validate that instead and lead with it, because a UNION proof is harder to dispute.",
		},
		References: []string{
			"https://github.com/r0oth3x49/ghauri",
			"https://portswigger.net/web-security/sql-injection/cheat-sheet",
			"https://cwe.mitre.org/data/definitions/209.html",
			"https://cwe.mitre.org/data/definitions/89.html",
			"https://owasp.org/www-project-web-security-testing-guide/latest/4-Web_Application_Security_Testing/07-Input_Validation_Testing/05-Testing_for_SQL_Injection",
		},
	},
	"ghauri|stacked queries": {
		Title:             "Stacked queries accepted, evidenced by a controlled delay in a second statement",
		WhatItProved:      "Ghauri got the backend to accept a second SQL statement after a semicolon. As with sqlmap, the confirmation is normally a CONDITIONAL TIME DELAY, because a stacked statement's result is never returned to the application: there is no output channel to read, so the only observable is how long the request took. What Ghauri observed is a response that took measurably longer when, and only when, the second statement was told to sleep. That is strong evidence the driver executes multiple statements per request, and it is a timing observation rather than returned output. Read the stored payload to confirm which discriminator was used before you validate.",
		WhatItDidNotProve: "No write was executed. Nothing was inserted, updated, deleted or dropped, and the database user's permission to do so was never tested. No data was retrieved through this technique. Operating system command execution was not demonstrated, however plausible it is on the underlying engine. Because the evidence is a delay, this finding carries every false-positive mode of time-based blind, and because Ghauri's output is scraped from stdout, the individual timing samples it took are not preserved in the finding.",
		WhyItMatters:      "This is the highest-impact technique Ghauri reports and the framework grades it critical, because a second arbitrary statement is not restricted to SELECT. Integrity and availability join confidentiality: data modification, account creation, privilege changes, table drops, and on some engines a path to the host. It also implies the application is not parameterising anywhere near this query and the driver has multi-statement execution enabled, which is systemic rather than a single bad concatenation.",
		FalsePositive:     "A slow or intermittently slow target, where the baseline spread overlaps the delay claimed. Second mode: a WAF or bot filter adding a fixed penalty delay to suspicious input. Symptom: the delay is constant regardless of the number asked for, so the scaling control does not scale and the zero control still delays. Third mode: scan concurrency exhausting the connection pool, making everything slow while the run is in progress. Symptom: baseline requests also slow during the scan and recover after. Fourth mode: the delay originating in the first statement, making this a mislabelled time-based finding; only the semicolon comparison catches it. Fifth mode, Ghauri-specific: a session verdict replayed from an earlier run on the same host when --flush-session was omitted.",
		SeverityNote:      "Critical because the demonstrated capability is arbitrary statement execution, not because arbitrary statements were executed. State plainly in the report that the evidence is a controlled, scaling delay from a statement after a semicolon and that no write was performed. If the semicolon comparison fails, downgrade to time-based blind and explain why.",
		ValidationSteps: []string{
			"Baseline the timing. Send the unmodified request at least five times, recording every one: curl -s -G \"https://TARGET/PATH\" --data-urlencode \"param=value\" -o /dev/null -w \"%{time_total}\\n\". Note the median and the worst case.",
			"Send the stacked payload with the sleep intact. The time must exceed your worst-case baseline by close to the full sleep duration.",
			"Scaling control, the step that decides the finding. Change the sleep to 2 seconds, then 8 seconds. The measured time must follow the number you chose. A slow endpoint, a tarpitting filter or a rate limiter cannot track a number you picked, and Ghauri's own confirmation logic works the same way.",
			"Zero control. Send the identical payload with the sleep set to 0 and confirm it returns at baseline, ruling out the payload's shape or length as the cause.",
			"Separate stacked from plain time-based, which is the whole distinction of this kind. Put the delay where only a second statement can reach it, for example value'; SELECT pg_sleep(5)--, and compare it against a delay nested in the original statement, for example value' AND (SELECT pg_sleep(5)) IS NOT NULL--. Only the semicolon form delaying proves stacking. If both delay, the technique attribution is wrong and this is time-based blind.",
			"Repeat the scaling control and the semicolon comparison three times each and keep the raw numbers for the report.",
			"Confirm Ghauri was run with --flush-session, or rerun it yourself with that flag. Its session is keyed by host and a replayed verdict is not a measurement.",
			"Stop there. Do not run a write statement to confirm capability. If proof of write is required, get explicit written permission and use the most reversible statement possible on an object you created.",
		},
		References: []string{
			"https://github.com/r0oth3x49/ghauri",
			"https://github.com/sqlmapproject/sqlmap/blob/master/data/xml/payloads/stacked_queries.xml",
			"https://portswigger.net/web-security/sql-injection/cheat-sheet",
			"https://cwe.mitre.org/data/definitions/89.html",
		},
	},
	"ghauri|time-based blind": {
		Title:             "Time-based blind SQL injection, confirmed by Ghauri varying the sleep duration",
		WhatItProved:      "Response time moved under Ghauri's control. Ghauri injects DBMS-specific sleep functions and, by its own design, uses VARYING sleep times to confirm rather than trusting one slow response: the true arm delays by approximately the requested amount, the false arm does not, and the delay tracks the number requested. That establishes a one-bit-per-request oracle signalled through timing rather than content, which is normally why this technique was reached at all.",
		WhatItDidNotProve: "No data was read. There is usually no content difference to corroborate the timing, which is precisely why timing was used, so the timing is the entire case. It does not prove exploitation is practical: at five seconds per bit, a 32-character value costs roughly an hour of requests. Nothing about writes, nothing about schema, nothing about whether an output-returning technique exists on the same parameter. Ghauri's individual timing samples are not preserved in the finding, since it has no machine-readable report, so the numbers you put in a report must be numbers you measured.",
		WhyItMatters:      "It is still full read access to whatever the database user can SELECT, at the worst extraction rate in the set. It is also the weakest evidence in the set and the finding most likely to be argued down, so it demands the strongest manual confirmation of any kind here. The framework grades it medium for that reason, which is a statement about evidence and cost rather than about the flaw.",
		FalsePositive:     "A slow or variable endpoint whose baseline maximum overlaps the claimed delay. Second mode: a filter that penalises suspicious input with a constant pause, so the scaling control does not scale. Third mode: rate limiting engaging mid-scan so all later requests are slower, including to the unmodified URL. Fourth mode: the scanner's own concurrency saturating the target, so the delay disappears when tested by hand. Fifth mode: network jitter on a single sample, which simply does not repeat. Sixth mode, Ghauri-specific: a per-host session verdict replayed without any request being sent.",
		SeverityNote:      "Medium in this framework, reflecting evidence strength and exploitation cost rather than the class. If you confirm extraction of a real value, or if the content-oracle hunt succeeds, the finding is genuinely a high and should be re-reported as boolean-based blind with the differential attached. Never submit a time-based finding backed by one measurement.",
		ValidationSteps: []string{
			"Baseline with ten samples, not three: curl -s -G \"https://TARGET/PATH\" --data-urlencode \"param=value\" -o /dev/null -w \"%{time_total}\\n\" run ten times. Record the median and the maximum, because you are about to argue about seconds.",
			"Send the true arm with the sleep intact and record the time.",
			"Send the FALSE arm: the same payload with the inner condition changed so it evaluates false. The delay is conditional, so this must return at baseline. This separates \"the database slept because I asked\" from \"this endpoint is slow\".",
			"Scaling control. Ask for 2 seconds, then 8, then 2 again. The measured delay must follow the number every time. This is the same thing Ghauri does internally and it is the strongest single piece of evidence for this kind.",
			"Repeat the false arm and the scaling control three times each and keep every number. Put the table in the report, not one screenshot of one slow response.",
			"Hunt for a content oracle instead. Send an obviously true and an obviously false condition with NO sleep and diff the bodies byte for byte. If they differ at all you have a boolean oracle: validate and report that, because it is cheaper to prove and far harder to dispute.",
			"Rule out the security stack. Send an equally long, equally unusual value containing no SQL syntax and confirm it returns at baseline.",
			"Confirm --flush-session was set. Without it Ghauri replays a per-host verdict, and a replayed verdict has no timing behind it at all.",
		},
		References: []string{
			"https://github.com/r0oth3x49/ghauri",
			"https://deepwiki.com/r0oth3x49/ghauri/4-sql-injection-detection",
			"https://portswigger.net/web-security/sql-injection/blind",
			"https://owasp.org/www-community/attacks/Blind_SQL_Injection",
			"https://cwe.mitre.org/data/definitions/89.html",
		},
	},
	"nomore403|*": {
		Title:             "Access control bypass candidate: a status code changed, which is not the same as access",
		WhatItProved:      "Forbidden re-sent a request differing from the refused one by a header, a path mutation or a verb, and got a different status, usually 200, whose length did not match the denial page.",
		WhatItDidNotProve: "That the protected resource came back. Its only content baseline is the ORIGINAL DENIAL: it never compares the response against the ordinary, no-header response of the URL it actually requested. So an allowed page returned under a header that did nothing looks exactly like a real bypass.",
		WhyItMatters:      "Where it is real, a caller reaches something the application meant to refuse. This class is almost always a split between where the check is applied, the request path, and where the routing decision is made, the header.",
		FalsePositive:     "The common case, so expect it. The tool requested a DIFFERENT, allowed URL with an extra header that changed nothing and got that allowed page back; it survived only because the length differs from the denial page. On ginandjuice.shop two of the three reported bypasses were exactly this.",
		SeverityNote:      "Medium as reported, because it is a candidate. High or critical only once you can name a string in the body that the denial withheld.",
		ValidationSteps: []string{
			"Fetch the protected path plainly and confirm it is still refused. Note the status and length.",
			"NEGATIVE CONTROL, the step that kills most findings in this family: fetch the finding's own URL with NO extra header.",
			"Run the finding's request, the curl above.",
			"Compare the result against the CONTROL first, before comparing it against the denial. Identical bodies mean the header did nothing and the finding is dead.",
			"If it survives, name the privileged string: point at something in the body the denial withheld, such as a username or an admin control. Byte length alone is not enough, since pages vary on their own.",
		},
		References: []string{
			"https://portswigger.net/web-security/access-control",
			"https://cwe.mitre.org/data/definitions/284.html",
		},
	},
	"nomore403|access-control-bypass": {
		Title:             "Access control bypass candidate: a status code changed, which is not the same as access",
		WhatItProved:      "Forbidden re-sent a request differing from the refused one by a header, a path mutation or a verb, and got a different status, usually 200, whose length did not match the denial page.",
		WhatItDidNotProve: "That the protected resource came back. Its only content baseline is the ORIGINAL DENIAL: it never compares the response against the ordinary, no-header response of the URL it actually requested. So an allowed page returned under a header that did nothing looks exactly like a real bypass.",
		WhyItMatters:      "Where it is real, a caller reaches something the application meant to refuse. This class is almost always a split between where the check is applied, the request path, and where the routing decision is made, the header.",
		FalsePositive:     "The common case, so expect it. The tool requested a DIFFERENT, allowed URL with an extra header that changed nothing and got that allowed page back; it survived only because the length differs from the denial page. On ginandjuice.shop two of the three reported bypasses were exactly this.",
		SeverityNote:      "Medium as reported, because it is a candidate. High or critical only once you can name a string in the body that the denial withheld.",
		ValidationSteps: []string{
			"Fetch the protected path plainly and confirm it is still refused. Note the status and length.",
			"NEGATIVE CONTROL, the step that kills most findings in this family: fetch the finding's own URL with NO extra header.",
			"Run the finding's request, the curl above.",
			"Compare the result against the CONTROL first, before comparing it against the denial. Identical bodies mean the header did nothing and the finding is dead.",
			"If it survives, name the privileged string: point at something in the body the denial withheld, such as a username or an admin control. Byte length alone is not enough, since pages vary on their own.",
		},
		References: []string{
			"https://portswigger.net/web-security/access-control",
			"https://cwe.mitre.org/data/definitions/284.html",
		},
	},
	"pphack|*": {
		Title:             "Client-side prototype pollution: the pollution is proven, the impact is not",
		WhatItProved:      "pphack loaded the page in a headless browser with a crafted query string and read the injected property back off Object.prototype. The pollution itself is observed, not inferred.",
		WhatItDidNotProve: "Any impact. Polluting the prototype is a PRIMITIVE. It becomes a vulnerability only when some code reads a property it never set and does something dangerous with it, which is called a gadget.",
		WhyItMatters:      "With a gadget this becomes DOM XSS, a script load from an attacker-controlled origin, or a client-side logic bypass. Without one it is a latent weakness.",
		FalsePositive:     "The page sets the property itself, so it appears on an object with no pollution having occurred. Loading the same page without the payload is what tells the two apart.",
		SeverityNote:      "High once a gadget is found, otherwise report it as a primitive and say so. pphack is NOT deterministic: measured at 3 hits in 6 identical runs against a URL it does detect, so one clean check refutes nothing.",
		ValidationSteps: []string{
			"Open the URL in a browser. curl cannot check this: the pollution happens in the page's own JavaScript, so the returned HTML is identical either way.",
			"In the console, read the injected property back off the prototype. The reproduction steps name this finding's key, which pphack randomises per run.",
			"Confirm it is the PROTOTYPE and not one object: an empty object literal should inherit the value.",
			"Load the same page WITHOUT the payload and repeat. The property must be absent.",
			"If the check fails, reload a few times before concluding anything: pphack is flaky and so is the underlying race.",
			"To turn it into an impact, find a gadget: somewhere the page reads a property it never set. On this target searchLogger.js reads config.transport_url and assigns it to a script src.",
		},
		References: []string{
			"https://portswigger.net/web-security/prototype-pollution",
			"https://cwe.mitre.org/data/definitions/1321.html",
		},
	},
	"pphack|client-side-prototype-pollution": {
		Title:             "Client-side prototype pollution: the pollution is proven, the impact is not",
		WhatItProved:      "pphack loaded the page in a headless browser with a crafted query string and read the injected property back off Object.prototype. The pollution itself is observed, not inferred.",
		WhatItDidNotProve: "Any impact. Polluting the prototype is a PRIMITIVE. It becomes a vulnerability only when some code reads a property it never set and does something dangerous with it, which is called a gadget.",
		WhyItMatters:      "With a gadget this becomes DOM XSS, a script load from an attacker-controlled origin, or a client-side logic bypass. Without one it is a latent weakness.",
		FalsePositive:     "The page sets the property itself, so it appears on an object with no pollution having occurred. Loading the same page without the payload is what tells the two apart.",
		SeverityNote:      "High once a gadget is found, otherwise report it as a primitive and say so. pphack is NOT deterministic: measured at 3 hits in 6 identical runs against a URL it does detect, so one clean check refutes nothing.",
		ValidationSteps: []string{
			"Open the URL in a browser. curl cannot check this: the pollution happens in the page's own JavaScript, so the returned HTML is identical either way.",
			"In the console, read the injected property back off the prototype. The reproduction steps name this finding's key, which pphack randomises per run.",
			"Confirm it is the PROTOTYPE and not one object: an empty object literal should inherit the value.",
			"Load the same page WITHOUT the payload and repeat. The property must be absent.",
			"If the check fails, reload a few times before concluding anything: pphack is flaky and so is the underlying race.",
			"To turn it into an impact, find a gadget: somewhere the page reads a property it never set. On this target searchLogger.js reads config.transport_url and assigns it to a script src.",
		},
		References: []string{
			"https://portswigger.net/web-security/prototype-pollution",
			"https://cwe.mitre.org/data/definitions/1321.html",
		},
	},
	"sqlidetector|*": {
		Title:             "SQLiDetector finding: a triage lead, never a confirmed injection",
		WhatItProved:      "SQLiDetector appended each of its 14 payloads to a query parameter value, issued plain HTTP GETs, and matched the responses against the DBMS error regexes in txt/sql_errors.txt, taken from sqlmap's error database. Every finding it produces is an error-message match; the framework's parser hardcodes the kind to \"error-signature\" for that reason, so any other kind from this tool would mean the parser changed and should be read with suspicion. Nothing in this tool performs a differential, evaluates a condition, measures timing, or reads a value back, so no finding from it can prove an injection by construction. The framework records every row as UNCONFIRMED.",
		WhatItDidNotProve: "No injection, on any row, ever. Beyond that: it sends requests.get only, so it never tested a request body, a method other than GET, a cookie as an injection point, or a header value. Its CLI also drops any target that does not contain a \"?\", so parameterless endpoints were skipped before the scan began. Only query vectors were eligible; every other insertion point on the same endpoint is neither clean nor tested. It does not attribute a hit to a single parameter in the stored param field, which carries the vector's whole parameter list. And rows are deduplicated by error signature, so the number of rows reflects how many distinct error patterns matched, not how many payloads worked.",
		WhyItMatters:      "Used correctly it is a cheap pointer that tells sqlmap and Ghauri where to spend their time, and it surfaces verbose database errors that are reportable in their own right. Used incorrectly it is the most dangerous tool in this section in both directions. A wall of rows across a host usually means one generic error page, not many bugs. And its silence is worthless as assurance: on the reference target a bare single quote returns HTTP 500 with no database error text, so SQLiDetector reports nothing while sqlmap confirms both boolean-based blind and a UNION query on the same parameter. Clean here means only that no error string matched.",
		FalsePositive:     "Framework or ORM exception pages returned for any malformed input; WAF and CDN block pages containing vendor strings that match a pattern; validation messages that echo your payload text back, so your own input is what matched; and pages that legitimately contain database vocabulary. Symptom common to all of them: a value with no SQL syntax reproduces the identical page. At scale, the signature is a large block of rows on one host all pointing at the same error page.",
		SeverityNote:      "Stored at medium with an explicitly unconfirmed confidence string, but treat every row as informational until a differential, a scaling delay, or returned output confirms it. Do not submit a SQLiDetector row as a SQL injection report. Do not treat its absence as evidence of absence.",
		ValidationSteps: []string{
			"Treat every row as a hypothesis. Go to the error-signature entry for the full procedure; the short version follows.",
			"Fetch the finding's URL field, which is the injected URL verbatim, and confirm the matched error text is genuinely in the body.",
			"Run the generic-error control: send the same parameter a malformed value with no SQL metacharacters and see whether the identical page comes back. If it does, discard the row.",
			"Attribute the hit to one parameter by reading the injected URL, then re-test parameters individually if the vector had several.",
			"Confirm with a real technique before believing anything: a true/false differential, a scaling time delay, or database-computed output in the response. sqlmap and Ghauri exist in this section to do exactly that.",
			"Group rows before triaging. If many rows across one host share the same error signature and the same page shape, treat them as one candidate issue rather than many.",
			"Never conclude a target is clean from a clean SQLiDetector result. Check whether sqlmap and Ghauri also ran against the same vector, and check that they actually reached the insertion point: cookie, header and path vectors are only tested when the value carries the literal * marker.",
			"If a real, verbose database error is confirmed, report the information exposure separately even when the injection does not hold up.",
		},
		References: []string{
			"https://github.com/eslam3kl/SQLiDetector",
			"https://github.com/sqlmapproject/sqlmap/wiki/Techniques",
			"https://owasp.org/www-project-web-security-testing-guide/latest/4-Web_Application_Security_Testing/07-Input_Validation_Testing/05-Testing_for_SQL_Injection",
			"https://cwe.mitre.org/data/definitions/209.html",
			"https://portswigger.net/web-security/sql-injection",
		},
	},
	"sqlidetector|error-signature": {
		Title:             "Database error message returned, UNCONFIRMED and not an injection",
		WhatItProved:      "One of SQLiDetector's 14 payloads was appended to a query parameter value, sent as a plain HTTP GET, and the response body matched one of the DBMS-error regexes it borrows from sqlmap's error database (txt/sql_errors.txt, currently 153 lines). That is the entire claim: a string that looks like a DBMS error appeared in a response to malformed input. The stored url AND the stored payload are both the exact injected URL, so unlike the sqlmap and Ghauri findings this row is directly pasteable. The finding is deduplicated by error SIGNATURE, so the URL shown is one of possibly several that matched the same pattern; the others were dropped as noise, not because they failed.",
		WhatItDidNotProve: "No injection. A parameter concatenated into a query and then correctly escaped downstream produces the same error, and so does an application whose exception handler prints on any malformed input. No differential was performed, no condition was evaluated, no value was returned, and no timing was measured. It also did not identify WHICH parameter is responsible in the param field: that field is the vector's whole parameter list joined together, and only the injected URL tells you where the payload actually went. It tested query parameters over GET only (requests.get is the only method in the tool), with no cookies as an injection point, no request body, no method override and no headers that are fuzzed; the CLI additionally discards any target URL that does not contain a \"?\", so parameterless endpoints were never looked at.",
		WhyItMatters:      "This is a triage pass, and it is useful in both directions if you read it correctly. A hit is a cheap pointer telling sqlmap or Ghauri where to spend time. Its SILENCE proves nothing at all: on the reference target a bare single quote returns HTTP 500 with a full HTML page containing no database error text (verified live), so SQLiDetector finds nothing there while sqlmap and Ghauri both confirm boolean-based blind and sqlmap additionally confirms a UNION query on the same parameter. Treating a clean SQLiDetector result as evidence of safety is the specific mistake this tool invites. Separately, if a real database error text is reaching clients, that verbose-error exposure is worth reporting on its own even when the injection is later disproved.",
		FalsePositive:     "The overwhelmingly common case. A framework or ORM exception page rendered for any malformed input, containing a driver name that matches a regex. A WAF or CDN block page carrying a vendor string that trips a pattern. An application that echoes the offending value inside a validation message, so your payload text itself is what matched. A page that legitimately contains database vocabulary, for example documentation, an error-code reference, or an admin log viewer. Symptom for all of these: the no-metacharacter control reproduces the identical page. Also expect a large number of these rows across a scan; a wall of matches on one host almost always means one generic error page, not many injections.",
		SeverityNote:      "The framework stores this at medium with the confidence string \"unconfirmed: a database error appeared, which is a lead rather than an injection. Confirm with sqlmap or Ghauri\". Treat it as informational until a differential, a scaling delay, or database-computed output confirms it. Never submit this row as a SQL injection report on its own; it will be closed as informative and it costs signal-to-noise ratio. Do not treat the absence of these rows as evidence a target is not injectable.",
		ValidationSteps: []string{
			"Open the finding's URL field, which is the injected URL verbatim, and fetch it: curl -s -i \"URL\". Confirm the error text is really in the body and quote it exactly.",
			"Negative control for generic error handling. Send the same parameter a value that is malformed but contains no SQL metacharacters at all, for example a long run of letters, or a percent sign, or a bracket. If the same error page appears, the application errors on bad input generally and the signature says nothing about SQL.",
			"Identify the responsible parameter. If the finding's param field lists more than one name, read the injected URL to see which value carries the payload, then re-test each parameter individually. Do not carry the multi-name label into a report.",
			"Confirm the error text is genuinely from a database rather than a framework or a WAF. Look for a DBMS product name, an SQLSTATE code, a driver class name or a query fragment. A 500 page that merely contains a matching word is not a database error.",
			"Test whether the error is input-driven at all. Send the unmodified parameter value and confirm you get a normal 200, so you can show the error is caused by your input and not by a pre-existing outage.",
			"Escalate to a real differential before you believe anything. Send a matched true/false pair on the same parameter, for example value' AND 1=1-- and value' AND 1=2--, and compare status and byte count as described in the boolean-based blind entry. Only that comparison, or a scaling time delay, or database-computed output in the body, upgrades this from a lead to an injection.",
			"Check whether sqlmap or Ghauri reported anything on the same vector. If they did, report their finding and discard this row. If they did not, decide whether the scan actually reached this parameter before concluding it is clean.",
			"If the error text turns out to be genuine and verbose, report the information exposure separately under CWE-209 even if the injection does not stand up.",
		},
		References: []string{
			"https://github.com/eslam3kl/SQLiDetector",
			"https://owasp.org/www-project-web-security-testing-guide/latest/4-Web_Application_Security_Testing/07-Input_Validation_Testing/05-Testing_for_SQL_Injection",
			"https://cwe.mitre.org/data/definitions/209.html",
			"https://cwe.mitre.org/data/definitions/89.html",
			"https://portswigger.net/web-security/sql-injection",
		},
	},
	"sqlmap|*": {
		Title:             "sqlmap finding: how to read any of them",
		WhatItProved:      "sqlmap reached a confirmation phase for one technique against one parameter. Its detection is two-stage: a candidate payload is tried, then a separate confirmation round re-tests the discriminator before anything is written to the report. A row in the report means that confirmation passed, not that one payload looked interesting. Each stored row is ONE technique against ONE parameter, so several rows on the same parameter are several independent confirmations rather than duplicates. The insertion point shown is the framework's own, taken from the attack vector rather than from sqlmap's place field, because sqlmap labels a marked cookie as \"(custom) HEADER\"; the parameter name, by contrast, IS sqlmap's own parameter field, and sqlmap's place is preserved in detection_method so the disagreement stays visible instead of silently overwriting ours.",
		WhatItDidNotProve: "Whether data was extracted depends on the run's settings, so check them rather than assuming. The framework adds no enumeration flags of its own (its framework-owned flags are --batch, --disable-coloring, --report-json, --output-dir and -v 0), but --dbs, --tables, --columns, --dump, --current-db, --banner, --users, --privileges and --schema are all operator-settable options in the Enumeration group. If they were left off, nothing was read out of the database and the evidence is detection only. The finding does not prove the injection is reachable by an unauthenticated attacker if the scan carried a session cookie or auth header. It does not prove the same behaviour on another environment, another host, or behind a different WAF profile. The stored payload is the technique's representative vector and is frequently only one arm of a matched pair, so pasting it into a browser on its own is not a reproduction and will usually look completely normal.",
		WhyItMatters:      "sqlmap is the highest-confidence detector in this section, and its technique names carry precise and very different meanings that decide what you do next. Techniques that RETURN data (UNION query, error-based, inline query) hand you a screenshot in one request. Techniques that INFER one bit at a time (boolean-based blind, time-based blind) reach the same data at a far higher cost, roughly seven to eight requests per character. Stacked queries is the only one that means statements other than SELECT. Reading the kind badge is the first triage decision, not a label.",
		FalsePositive:     "A response comparison performed against a page with per-request variable content: a rotating banner, a CSRF token, a timestamp, an ad slot, a request ID. This is the root cause of nearly every disputed sqlmap finding. Symptom: two identical requests return two different byte counts. Also: a finding produced by a run that carried credentials but reported as unauthenticated; a confirmation that rested on timing against an unstable or rate-limited target; and the same underlying injection reported once per URL variant, which reads as several bugs but is one. Symptom for the last: identical payload and identical technique on URLs that differ only in an unrelated query parameter.",
		SeverityNote:      "Severity is assigned by technique, not by the class: stacked queries critical; UNION query, error-based, boolean-based blind and inline query high; time-based blind medium. That ranking encodes what the technique demonstrates and how cheap extraction is. It is not a statement that some SQL injection is minor.",
		ValidationSteps: []string{
			"Read the technique in the kind badge and go to that technique's entry. The validation procedure for each is genuinely different and they are not interchangeable.",
			"Establish your control first. Send the unmodified request twice and record status, byte count and time: curl -s -G \"https://TARGET/PATH\" --data-urlencode \"param=originalvalue\" -o /dev/null -w \"%{http_code} %{size_download} %{time_total}\\n\". Every technique's proof is a comparison against this. If the two runs disagree on byte count, the page is not stable and content comparison is not a valid oracle; pin the comparison to one specific string in the body instead.",
			"Reproduce outside the tool. Send the exact stored payload by hand with curl or Burp Repeater, URL encoding it, and confirm you see what sqlmap saw. Never report from the tool's word alone.",
			"Check what the scan carried. If a session cookie or auth header was supplied, repeat the reproduction with it removed and record whether the injection is pre-auth or post-auth. This changes severity and is the single most common gap in a submitted report.",
			"Check whether the run had enumeration options enabled before writing \"no data was extracted\". If --dump or --schema was on, data was read and that belongs in the report and in your handling of it.",
			"Confirm the parameter and insertion point in the finding match the request you are sending. Cookie, header and path vectors reach sqlmap through a literal * marker on the value, so the stored payload for those will not look like a normal query string, and the parameter name shown is sqlmap's label for the marked input.",
			"If several techniques are reported for the same parameter, validate the one that RETURNS data first (UNION query, error-based, inline query). It is the cheapest to prove and the hardest for a triager to dispute.",
			"Do not run enumeration or dumping to \"confirm\" the finding unless the program's rules allow it. None of these findings require it, and a harmless value such as the DBMS version is enough to establish impact.",
		},
		References: []string{
			"https://github.com/sqlmapproject/sqlmap/wiki/Techniques",
			"https://github.com/sqlmapproject/sqlmap/wiki/Usage",
			"https://portswigger.net/web-security/sql-injection",
			"https://cwe.mitre.org/data/definitions/89.html",
			"https://owasp.org/www-project-web-security-testing-guide/latest/4-Web_Application_Security_Testing/07-Input_Validation_Testing/05-Testing_for_SQL_Injection",
		},
	},
	"sqlmap|UNION query": {
		Title:             "UNION-based SQL injection, confirmed by database-computed output rendered in the response",
		WhatItProved:      "sqlmap appended a UNION arm to the original query and its own marker came back in the response body. The marker matters more than it looks: in a payload such as UNION ALL SELECT NULL,NULL,(CHR(113)||CHR(122)||CHR(118)||CHR(106)||CHR(113))||... it appears in the request only as a CHR() arithmetic expression, and in the response as plain text. A reflection of your request cannot produce the decoded string, so the database computed it and the application rendered it. The payload also records the query's shape: NULL,NULL,marker,NULL,NULL,NULL,NULL,NULL,NULL means the original SELECT returns nine columns and column three reaches the page as text. The || concatenation and CHR() form indicate a PostgreSQL or Oracle family backend.",
		WhatItDidNotProve: "No real data was read unless enumeration options were enabled on the run, which you should check. The value returned was a string sqlmap invented. No table, column or row was shown to be readable. It proves nothing about write access. And the evidence line \"Generic UNION query (NULL) - 1 to 20 columns\" describes the range sqlmap searched, not a property of the application; the actual column count is the number of items in the payload, which you should read off it rather than off the title.",
		WhyItMatters:      "UNION returns data in the response body, so extraction costs one request per query instead of one request per bit. Everything the database user can SELECT is a single request away, including other tenants' rows when the table is shared. This is the technique that turns \"the parameter is injectable\" into a screenshot of a customer table, and it is the one to lead a report with when it is present alongside blind findings.",
		FalsePositive:     "The marker echoed back because the application prints the raw parameter value, for example in a \"no results for X\" line or an error page. sqlmap's CHR() encoding is specifically what prevents this, so it only bites when a tamper script, an intercepting proxy, or a hand edit put the plain marker into the request. Symptom: the marker in the response is surrounded by the rest of your payload text rather than sitting alone in a table cell. Second mode: a cached page still carrying a marker from an earlier request. Symptom: change the marker and the page keeps showing the old one. Third mode: apparent success at every column count, which means nothing is actually being rendered from your injected arm. Symptom: the column-count control never fails.",
		SeverityNote:      "High, and this is the finding to put at the top of a report because the evidence is a value the database returned rather than an inference a tool made. Raise to critical only when you have actually retrieved data belonging to other users, credentials, or secrets, and have attached that evidence. Do not raise it on the theoretical availability of a dump.",
		ValidationSteps: []string{
			"Send the stored payload URL encoded and read the response BODY, not just the status: curl -s -G \"https://TARGET/PATH\" --data-urlencode \"param=<the stored payload verbatim>\" -o u.html , then locate the text between sqlmap's two delimiter strings (in the reference payload, qzvjq and qzzxq).",
			"Reflection control, the single most important step here. Search the request you just sent for the plain marker string. It is not there; it exists only as CHR() arithmetic. If you can find the literal decoded string anywhere in your own request line, headers or body, you are looking at reflection and the finding is not proved.",
			"Prove the database computes an arbitrary value. Replace the marker expression with CHR(65)||CHR(66)||CHR(67) and confirm ABC appears in the page while the text CHR(65) does not. Change it to CHR(88)||CHR(89)||CHR(90) and confirm the page now says XYZ. The rendered text must track the arithmetic you sent.",
			"Pin the column count to the application rather than to sqlmap's search. Keep the same number of columns and move the marker one position along; note whether it still renders and which positions reach the page. Then change the count by one in each direction and confirm you get an error or an empty result. If every column count \"works\", the page is not rendering your UNION at all and the previous step was reading something else.",
			"Establish impact with one harmless real value. Substitute a DBMS function for the marker: version() on PostgreSQL, @@version on MySQL or MSSQL, banner FROM v$version on Oracle. Screenshot the page showing it. Prefer version, current database name or current user over anything belonging to an application user.",
			"Syntax control. Remove the trailing comment token and confirm the request now errors or returns a different page. If the page renders identically with the payload truncated mid-syntax, the parameter may be ignored entirely and you are looking at a static page.",
			"If the scan carried a session cookie or auth header, repeat the impact step without it and record whether the injection is pre-auth.",
		},
		References: []string{
			"https://portswigger.net/web-security/sql-injection/union-attacks",
			"https://portswigger.net/web-security/sql-injection/cheat-sheet",
			"https://github.com/sqlmapproject/sqlmap/blob/master/data/xml/payloads/union_query.xml",
			"https://cwe.mitre.org/data/definitions/89.html",
		},
	},
	"sqlmap|boolean-based blind": {
		Title:             "Boolean-based blind SQL injection, confirmed by a true/false response differential",
		WhatItProved:      "sqlmap sent two requests whose payloads were identical except for the truth value of an appended SQL condition, and the two responses differed in a way that survived its page-stability and dynamic-content checks. The stored payload is only the TRUE arm. For a payload such as category=Accessories' AND 5470=5470 AND 'yLfC'='yLfC, sqlmap also sent the FALSE twin (5470=5471) and compared the two. The difference proves the appended text was parsed and evaluated as SQL by the backend, and that the result of that evaluation changes what the application returns to you. That is a working oracle: one arbitrary yes/no question about the database per request.",
		WhatItDidNotProve: "No data came back, unless the run had enumeration options enabled, which you should check rather than assume. Boolean-based blind returns nothing by design; it answers one bit at a time. Nothing was established about which tables or columns exist, how many rows are reachable, or what privileges the database user holds. It does not prove the parameter is injectable without whatever cookies or headers the scan carried. Critically, the finding as stored is not a reproduction: the payload in this modal, sent on its own, produces a page that looks entirely normal, because it is the arm that evaluates true. The differential is the proof, and half of it was never recorded.",
		WhyItMatters:      "A boolean oracle is full read access to everything the database user can SELECT. It is slow, not limited: roughly seven to eight requests per character with a binary search, and sqlmap will do the walking for you. The confidentiality impact is identical to a UNION finding; only the extraction cost differs. Because the injected condition is genuinely evaluated, the same insertion point will usually accept other constructs too, so this finding is also the entry point for checking UNION, error-based and stacked queries.",
		FalsePositive:     "A page whose content varies per request, so any two responses differ and any naive diff calls everything injectable. sqlmap strips dynamic regions but does not always succeed. Symptom: the stability control gives two different byte counts. Second mode: the application reflects the parameter value into the body, so the true and false arms differ by exactly the 5470 versus 5471 text you sent, and by nothing else. Symptom: diff the two bodies and the only delta is your own payload string. Third mode: a WAF or bot filter that blocks one arm and passes the other, so you are comparing a block page against a real page. Symptom: the two arms return different status codes or one body contains a vendor block message. Fourth mode: a cache serving one arm and computing the other. Symptom: the difference vanishes or inverts when you add a cache buster.",
		SeverityNote:      "High, and it should not be downgraded because \"no data came back\". Nothing was asked for. A confirmed boolean oracle carries the same confidentiality impact as UNION; the difference is time, not reach. Downgrade only if the parameter is reachable exclusively with credentials the scan supplied that a real attacker would not hold, and state that explicitly rather than silently lowering the score.",
		ValidationSteps: []string{
			"Stability control, before anything else. Send the unmodified request twice and record the byte count both times: curl -s -G \"https://ginandjuice.shop/catalog\" --data-urlencode \"category=Accessories\" -o /dev/null -w \"%{http_code} %{size_download}\\n\". Both runs must agree. If they disagree, byte count is not a usable oracle on this page; pick a stable string in the body and grep for it instead. Do not memorise a byte count from a write-up, including this one: the reference site's page sizes drift, so measure your own baseline every time.",
			"Send the TRUE arm exactly as stored: curl -s -G \"https://ginandjuice.shop/catalog\" --data-urlencode \"category=Accessories' AND 5470=5470 AND 'yLfC'='yLfC\" -o /dev/null -w \"%{http_code} %{size_download}\\n\". Record status and size.",
			"Derive the FALSE arm yourself by changing ONE digit so the condition is false, 5470=5470 becoming 5470=5471, leaving every other byte identical including the trailing quote-balancing clause. Send it and record status and size.",
			"Compare the three numbers. The true arm must match or closely track the baseline; the false arm must differ from the true arm. On the reference target the hand-built equivalent pair is ' AND 1=1-- returning the base page and ' AND 1=2-- returning a page with an empty product list, which is a DIFFERENT size from the base page. So \"false looks like baseline\" is the wrong test; the test is that true and false differ from each other and each repeats.",
			"Negative control for text shape. The two arms above differ by exactly one character and are the same length, so no difference in payload length, quoting or comment syntax can account for the change. Now confirm the app is not simply reacting to an unrecognised value: send category=AccessoriesZZZ with no SQL syntax at all. If that produces the same page as your FALSE arm, that is expected (both yield no rows); if it produces the same page as your TRUE arm, your oracle is not measuring SQL evaluation and the finding is unproved.",
			"Repeat the true and false arms once more each. Both must reproduce their own byte counts. A single non-repeating difference is noise, not an oracle.",
			"Demonstrate the oracle rather than the payload. Replace the constant comparison with an expression the database must compute and whose truth you can flip and predict, for example ' AND (SELECT 1)=1-- against ' AND (SELECT 1)=2--, then a length comparison against a subquery that is obviously true versus obviously false. The page must track the truth of the expression every time. Use the DBMS-appropriate form from the PortSwigger cheat sheet.",
			"For the report, extract one harmless real value to show impact, for example narrow the first character of the DBMS version with a handful of comparisons, or hand the confirmed parameter to sqlmap with --current-db only. Do not dump user data to prove a point.",
		},
		References: []string{
			"https://portswigger.net/web-security/sql-injection/blind",
			"https://portswigger.net/web-security/sql-injection/cheat-sheet",
			"https://github.com/sqlmapproject/sqlmap/blob/master/data/xml/payloads/boolean_blind.xml",
			"https://owasp.org/www-community/attacks/Blind_SQL_Injection",
			"https://cwe.mitre.org/data/definitions/89.html",
		},
	},
	"sqlmap|error-based": {
		Title:             "Error-based SQL injection, confirmed by a computed value returned inside a database error",
		WhatItProved:      "sqlmap forced the DBMS to raise an error whose text contains the result of an expression sqlmap chose, and the application rendered that error text in the HTTP response. Two separate things were established at once: the backend evaluated attacker-supplied SQL, and the application leaks raw database error text to the client. The value sitting between sqlmap's delimiters in the error string is the answer to the injected subquery, so this technique RETURNS data, one value per request, with no need to match the original query's column count.",
		WhatItDidNotProve: "Nothing about which data is reachable, unless enumeration options were enabled on the run. sqlmap asked for its own marker plus a probe value, not for your users table. It does not prove errors are exposed in every code path; the leak may exist only for this endpoint or this error class. It says nothing about whether UNION or stacked queries also work, and nothing about write access. It also does not prove the error page is reachable by an unauthenticated client if the scan was authenticated.",
		WhyItMatters:      "Error-based is the fastest extraction sqlmap has after UNION: roughly one value per request, no column matching, no bit-by-bit inference. Beyond the injection itself, verbose database errors reaching clients is a reportable finding on its own, and it hands an attacker the DBMS, the version, and often schema and column names for free. Note the absence case too: on the reference target a bare single quote on the catalog category parameter returns HTTP 500 with a full HTML error page that contains no database error text at all (verified live: zero matches for sql, postgres, syntax error, exception), which is exactly why error-based is not reported there even though the parameter is provably injectable. Absence of this kind next to a confirmed boolean finding is normal, not a contradiction.",
		FalsePositive:     "The application echoing the offending input into a generic 500 or \"invalid value\" page. Your marker comes back because you sent it, and a regex or a fast read mistakes that for extraction. The bare-quote negative control catches it. Second mode: a WAF, framework or CDN error page that happens to contain a phrase matching a DBMS error pattern. Symptom: the same page appears for any malformed value, including values with no injection syntax at all. Third mode: a returned value that never changes when you change the expression. Symptom: the expression-tracking step gives the same number twice.",
		SeverityNote:      "High. Data returns in the response body, so extraction is cheap and the proof is direct. The verbose error leak is an additional, separately reportable issue (CWE-209). Do not treat the absence of this kind as evidence that a target is not injectable; a 500 whose body carries no database text suppresses error-based detection entirely while leaving boolean and time techniques fully viable.",
		ValidationSteps: []string{
			"Send the stored payload and read the response BODY. Find sqlmap's delimiters and the value between them. If the body is generic or carries no database text, this kind cannot be supported by this response and the finding needs re-examination: error-based requires readable error text, not merely a 500 status.",
			"Prove the value tracks your expression rather than your input. Send the same payload twice with the embedded expression changed from something returning 1+1 to something returning 2+2 and confirm the returned value changes from 2 to 4. This is the control that separates evaluation from echo, and it is the one step you must not skip.",
			"Negative control for the echo case. Send a payload that breaks the query but contains no computable expression, for example a bare single quote, and confirm the resulting error page does NOT contain your marker string. If your marker appears anyway, the application is echoing your input into its error page and the \"returned value\" was never computed by anything.",
			"Retrieve one harmless real value to establish impact, using the DBMS-appropriate error form: on PostgreSQL cast a concatenation containing (SELECT version()) to NUMERIC; on MySQL use EXTRACTVALUE or the double-query form; on MSSQL use CONVERT to int. Screenshot the error page showing the value.",
			"Confirm the leak is not authenticated-only. Repeat the impact step with any session cookie removed, if the endpoint permits it.",
			"Record the exact error string verbatim and the DBMS fingerprint it reveals. The verbose error exposure is reportable separately from the injection, and having the string quoted saves an argument later.",
			"Re-send the unmodified request afterwards and confirm the application still behaves normally, so you can show the error condition is input-driven and not a pre-existing outage.",
		},
		References: []string{
			"https://portswigger.net/web-security/sql-injection/cheat-sheet",
			"https://github.com/sqlmapproject/sqlmap/blob/master/data/xml/payloads/error_based.xml",
			"https://cwe.mitre.org/data/definitions/209.html",
			"https://cwe.mitre.org/data/definitions/89.html",
			"https://owasp.org/www-project-web-security-testing-guide/latest/4-Web_Application_Security_Testing/07-Input_Validation_Testing/05-Testing_for_SQL_Injection",
		},
	},
	"sqlmap|inline query": {
		Title:             "Inline query SQL injection, confirmed by a nested subquery result rendered in the response",
		WhatItProved:      "sqlmap embedded a subquery inside the original statement, in a position where the application renders its result, and got its marker back in the response body. Like UNION this technique RETURNS data. Unlike UNION it does not require the column count to match, because the injected SELECT is nested inside the existing query rather than appended as a second result set. What was proved is that the backend evaluated the nested SELECT and the application printed its result to the client.",
		WhatItDidNotProve: "The value returned was sqlmap's own marker, not real data, unless enumeration options were enabled on the run. Nothing was established about which tables are readable, about write access, or about whether UNION or stacked queries would also work on the same parameter. sqlmap ships far fewer inline-query vectors than it does for the other techniques, so the absence of this kind on a parameter means very little and should not be read as a limit on the injection.",
		WhyItMatters:      "Same impact class as UNION: one request per value, output visible in the page, no bit-by-bit inference. It is often the technique that succeeds where UNION fails, for example when the injection point sits in an ORDER BY, a GROUP BY, or a column-name position where a second SELECT cannot be appended. Because it lands in positions developers assume are structural rather than data, it usually appears in code that was never reviewed for injection at all, and the fix is different: allow-listing the identifier rather than parameterising a value.",
		FalsePositive:     "The marker echoed from your request rather than computed by the database, which the reflection control and the arithmetic control exist to catch. Second mode: a cached response still holding the previous marker, so changing the marker does not change the page. Third mode: the application printing the parameter value in a \"showing results for X\" line, which puts your text in the body for reasons that have nothing to do with SQL. Symptom: the marker appears surrounded by the rest of your payload rather than isolated in a rendered field.",
		SeverityNote:      "High, the same as UNION query, because output is returned to the client. Do not let the unfamiliar technique name lead a triager to grade it below UNION; the extraction rate and the confidentiality impact are the same. If the position is an identifier or ORDER BY, note in the report that a parameterised query alone will not fix it.",
		ValidationSteps: []string{
			"Send the stored payload URL encoded and locate the marker inside the response body.",
			"Reflection control, exactly as important here as for UNION. Confirm the plain marker text does not appear anywhere in the request you sent. If sqlmap encoded it as CHR() or CONCAT of character codes, a reflection cannot produce the decoded string; if the plain string is in your request, the finding is unproved.",
			"Prove computation rather than echo. Replace the marker with arithmetic whose answer is not present in your request, for example a scalar subquery returning 61*3, and confirm 183 appears in the page while the text 61*3 does not. Change it to 61*4 and confirm the page now shows 244.",
			"Identify the injection position, because it changes both exploitation and the fix advice. If the payload only works as a complete scalar subquery wrapped in parentheses, the insertion point is a column reference or an ORDER BY expression rather than a quoted value. Test that directly: try appending a plain quoted-string payload and confirm it fails where the subquery form succeeds.",
			"Return one harmless real value to establish impact, for example the DBMS version or current database name, and screenshot it.",
			"Cache and staleness control. Send the same structure with a deliberately broken subquery and confirm you get an error or an empty rendering rather than the previous value. If the old marker persists, you are reading a cached page and the computation and impact steps are unreliable.",
			"If the scan carried credentials, repeat the impact step without them.",
		},
		References: []string{
			"https://github.com/sqlmapproject/sqlmap/blob/master/data/xml/payloads/inline_query.xml",
			"https://github.com/sqlmapproject/sqlmap/wiki/Techniques",
			"https://portswigger.net/web-security/sql-injection/cheat-sheet",
			"https://portswigger.net/web-security/sql-injection",
			"https://cwe.mitre.org/data/definitions/89.html",
		},
	},
	"sqlmap|stacked queries": {
		Title:             "Stacked queries accepted, evidenced by a controlled delay in a second statement",
		WhatItProved:      "sqlmap got the backend to accept a second, separate SQL statement after a semicolon. Read this evidence carefully, because it is not what the severity implies. Almost every vector in sqlmap's stacked_queries.xml confirms with a CONDITIONAL TIME DELAY, using [INFERENCE] and [SLEEPTIME] placeholders, precisely because a stacked statement's result is never returned to the application. What sqlmap actually observed is a response that took measurably longer when, and only when, the second statement was told to sleep. That is strong evidence the database driver executes multiple statements per request. It is a timing observation, not returned output. Read the stored payload: if it contains no sleep function, the confirmation used a different discriminator and you should identify which before validating.",
		WhatItDidNotProve: "sqlmap did not execute a write. Nothing was inserted, updated, deleted or dropped, and the database user's permission to do any of those was never tested. No data was retrieved through this technique; stacked statements return nothing to the page. It did not prove operating system command execution (xp_cmdshell on MSSQL, COPY TO PROGRAM on PostgreSQL), even though those are the usual escalation paths people assume from the word \"stacked\". And because the evidence is a delay, this finding inherits every false-positive mode of time-based blind.",
		WhyItMatters:      "This is the highest-impact technique in the set and the framework grades it critical, because a second arbitrary statement is not limited to SELECT. Integrity and availability enter scope alongside confidentiality: data modification, account creation, privilege changes, table drops, and on some engines a route to the operating system. It also means the application is not using parameterised statements anywhere near this query and the driver has multi-statement execution enabled, which is a systemic finding rather than a single bad string concatenation.",
		FalsePositive:     "A target that is simply slow, or intermittently slow under load. Symptom: your baseline has a wide spread that overlaps the delay you want to claim. Second mode: a WAF, rate limiter or bot-detection layer that adds a fixed penalty delay to suspicious input. Symptom: the delay is the same regardless of the number you asked for, so the scaling control does not scale and the zero control still delays. Third mode: the scan's own concurrency exhausting the connection pool, making everything slow while it runs. Symptom: unmodified baseline requests slow down too during the scan and recover afterwards. Fourth mode: the delay originating in the first statement rather than a second one, which makes this a mislabelled time-based finding. The semicolon comparison is the only thing that catches it.",
		SeverityNote:      "Critical in this framework because the demonstrated capability is arbitrary statement execution, not because arbitrary statements were executed. Be explicit in the report that the evidence is a controlled, scaling delay produced by a statement after a semicolon, and that no write was performed. Overclaiming here is the fastest route to a rejected report. If the semicolon comparison fails, downgrade this to time-based blind and say why.",
		ValidationSteps: []string{
			"Baseline the timing properly. Send the unmodified request at least five times and record every time: curl -s -G \"https://TARGET/PATH\" --data-urlencode \"param=value\" -o /dev/null -w \"%{time_total}\\n\". Write down the median AND the worst case.",
			"Send the stacked payload with the sleep intact and record the time. It must exceed your worst-case baseline by close to the full sleep duration, not by a fraction of it.",
			"Scaling control, the step that decides this finding. Change the sleep value to 2 seconds, then to 8 seconds. The measured response time must follow the number you chose each time. A slow endpoint, a tarpitting WAF, a rate limiter and a saturated connection pool cannot track a number you picked.",
			"Zero control. Send the same payload with the sleep set to 0. It must return at baseline. This rules out the payload's length, shape or weirdness being what slows the endpoint.",
			"Separate stacked from plain time-based, which is what actually distinguishes this kind. Put the delay where only a SECOND statement can reach it, for example value'; SELECT pg_sleep(5)--, and compare against a delay nested inside the original statement, for example value' AND (SELECT pg_sleep(5)) IS NOT NULL--. Only if the semicolon form delays have you shown stacking. If both delay, you have time-based blind and the technique attribution is wrong.",
			"Repeat the scaling control and the semicolon comparison at least three times each and keep the raw numbers. Timing evidence from a single sample is not evidence and a triager will say so.",
			"Stop there. Do not run a write statement to \"confirm\" it. If the program demands proof of write capability, get explicit written permission first and use the most reversible statement possible against a scratch object you created yourself.",
		},
		References: []string{
			"https://github.com/sqlmapproject/sqlmap/blob/master/data/xml/payloads/stacked_queries.xml",
			"https://github.com/sqlmapproject/sqlmap/wiki/Techniques",
			"https://portswigger.net/web-security/sql-injection/cheat-sheet",
			"https://cwe.mitre.org/data/definitions/89.html",
		},
	},
	"sqlmap|time-based blind": {
		Title:             "Time-based blind SQL injection, confirmed by a conditional delay under sqlmap's control",
		WhatItProved:      "Response time changed under sqlmap's control. sqlmap injected a conditional delay, the arm whose condition evaluated true took approximately the requested time longer, the false arm did not, and by default sqlmap validates that split with a statistical model across repeated samples rather than trusting one measurement. That establishes the same one-bit-per-request oracle boolean-based blind gives, but signalled through timing instead of content, which is normally why this technique was reached at all.",
		WhatItDidNotProve: "No data was read. No content difference was found, which is usually the reason this technique was used, so there is no page diff available as corroboration and the timing is the whole case. It does not prove exploitation is practical at a useful rate: at five seconds per bit, one 32-character value runs to roughly an hour of requests. It proves nothing about writes, nothing about the schema, and nothing about whether output-returning techniques would work on the same parameter.",
		WhyItMatters:      "It is still full read access to whatever the database user can SELECT, at the worst extraction rate of any technique in the set. In practice it is also the weakest evidence in the set and the finding most likely to be disputed, so it needs the strongest manual confirmation of any kind here. The framework grades it medium for exactly that reason, which is a statement about evidence strength and cost, not about the severity of the underlying flaw.",
		FalsePositive:     "A slow or variable endpoint where the baseline spread overlaps the delay being claimed. Symptom: the baseline maximum is within range of your \"delayed\" response. Second mode: a security layer that penalises suspicious input with a constant pause. Symptom: the scaling control does not scale, the delay is the same for 2 and 8 seconds. Third mode: rate limiting engaging partway through a scan so every later request is slower. Symptom: the effect appears only after N requests and also affects the unmodified URL. Fourth mode: the scanner's own concurrency saturating the target. Symptom: the delay disappears entirely when you test by hand. Fifth mode: plain network jitter on a single sample. Symptom: it does not repeat.",
		SeverityNote:      "Medium in this framework, reflecting evidence strength and exploitation cost rather than the vulnerability class. If you confirm extraction of a real value, or if the content-oracle hunt succeeds, the finding is genuinely a high and should be re-reported as boolean-based blind with the differential attached. Never submit a time-based finding backed by a single measurement.",
		ValidationSteps: []string{
			"Baseline hard. Send the unmodified request at least ten times, recording each time: curl -s -G \"https://TARGET/PATH\" --data-urlencode \"param=value\" -o /dev/null -w \"%{time_total}\\n\". Record the median and the maximum. Ten samples rather than three, because you are about to argue with someone about seconds.",
			"Send the TRUE arm of sqlmap's payload with the sleep intact and record the time.",
			"Send the FALSE arm: the same payload with the inner condition changed so it evaluates false. The delay is conditional, so a false condition means no sleep, and this request must return at baseline. This is the control that separates \"the database slept because I asked it to\" from \"this endpoint is slow\".",
			"Scaling control. Ask for 2 seconds, then 8 seconds, then 2 again. The measured delay must follow the number each time. Nothing outside your control tracks a number you chose, which makes this the strongest single piece of evidence available for this kind.",
			"Repeat the false arm and the scaling control three times each and keep every number. Put the table of measurements in the report rather than one screenshot of one slow response.",
			"Look for a content oracle you can use instead. Send an obviously true and an obviously false condition with NO sleep at all and diff the two bodies byte for byte. If they differ, you have a boolean oracle: validate and report that instead. It is faster, cheaper to prove, and far harder to dispute.",
			"Rule out the security stack. Send an equally long, equally unusual value containing no SQL syntax and confirm it returns at baseline. If that also delays, a filter is penalising odd input and your delay is not coming from the database.",
		},
		References: []string{
			"https://portswigger.net/web-security/sql-injection/blind",
			"https://owasp.org/www-community/attacks/Blind_SQL_Injection",
			"https://github.com/sqlmapproject/sqlmap/blob/master/data/xml/payloads/time_blind.xml",
			"https://portswigger.net/web-security/sql-injection/cheat-sheet",
			"https://cwe.mitre.org/data/definitions/89.html",
		},
	},
	"xssfuzz|*": {
		Title:             "xssFuzz, kind not yet catalogued: in practice only kind R is reachable today",
		WhatItProved:      "It depends entirely on which printed line the parser matched, and only one of the two currently matches anything the tool prints. Kind R comes from \"[+] Parameter NAME not handling dangerous characters properly!\", which is a substring test for the literal ><randomstring in one response, with no browser and no parsing. Kind V is defined in the parser as the line containing \"XSS Found\" or \"Vulnerable URL\", but xssFuzz emits neither string anywhere in its source. Its -V validation stage, which does drive a real headless Chromium through selenium (validate.py hardcodes options.binary_location = \"/usr/bin/chromium\", which is why the container installs the distro package), prints \"[+] XSS Validated at <url>\" from generatePayload and \"[+] XSS Validated on <url>\" from staticTesting. Its non-validated reflection hits print \"[+] Got Something! <url>\". So today an xssFuzz row in this framework is an R, and the tool's genuine browser-execution result is being dropped by the parser rather than stored. xssFuzz has no machine-readable output at all (-o writes the same lines it prints), which is why the parser matches literal wording in the first place.",
		WhatItDidNotProve: "xssFuzz can only reach a QUERY parameter. It calls requests.get with no method override anywhere in its source, its substitution is re.sub(f\"{param}=([^&]+)\") over the raw URL string, and its request-body (-r) and crawl-domain (-d) options exist in the source but are COMMENTED OUT. So no xssFuzz row is evidence about a body, header, cookie or path input regardless of what insertion_point says. Two structural facts also block direct reproduction: the parser stores vector.EvidenceURL in url rather than the URL xssFuzz actually requested, and it stores no payload for R rows even though the probe string is a fixed literal. Two more facts limit what -V could ever prove even once the parser is fixed: its validation loop breaks after the FIRST validated URL, so at most one execution result exists per run, and validate_js_alert waits a fixed three seconds and looks only for a JavaScript alert, so a payload that executes without raising an alert is recorded as clean.",
		WhyItMatters:      "The two kinds are separated by a browser, and in this framework the browser-backed one does not currently reach the modal, so everything you see from this tool is the weaker one. An operator who assumes a V could have appeared and did not, and reads that as \"the browser stage found nothing\", is reading a parser gap as a scan result.",
		FalsePositive:     "For a reflection row: the substring landing somewhere inert, such as a JavaScript string literal, an HTML comment, a <textarea>, or a JSON response no browser renders. There is also a structural shape that looks like a finding and is not: a row whose stored URL does not contain the named parameter, which means the url field is the vector's evidence URL rather than the request that produced the result. And an absence shape: no browser-execution row from this tool means the parser did not match, not that a browser looked and saw nothing.",
		SeverityNote:      "Split by detection_method, not by the letter. Reflection rows are low to informational until a browser confirms execution; the stored medium is a holding value. A genuine browser-execution result from xssFuzz would be a real execution claim and high once the negative control passes, at the severity the origin and session justify, but confirm it against the raw output first given the parser gap. Never merge the two into a single \"xssFuzz found XSS\" line in a report.",
		ValidationSteps: []string{
			"Read detection_method on the row. \"reflection\" means the substring stage and no browser. \"browser-execution\" would mean the -V stage, but see the note above: the parser's trigger strings do not match what xssFuzz prints, so treat a browser-execution row from this tool as an anomaly worth checking against the raw scan output before believing it.",
			"For a reflection row, reconstruct the request by hand. The probe string is always the literal ><randomstring : curl -sG \"https://TARGET/PATH\" --data-urlencode \"PARAM=><rs0nMARKER\" -o r.html ; grep -n rs0nMARKER r.html . Inspect every place the marker lands and classify each context.",
			"Do the browser step yourself rather than waiting for the tool to do it. Build a payload appropriate to the context you just classified, load it with DevTools open, and confirm the alert.",
			"Negative control for either kind: request the same URL with an inert marker carrying no special characters. If the response and the browser behave identically, the finding added nothing.",
			"Confirm the parameter was actually substituted. re.sub(f\"{param}=([^&]+)\") requires the parameter to be present in the URL with a non-empty value. If it was absent, xssFuzz requested the URL unchanged and any match came from the page itself.",
			"If a row's insertion_point is anything other than query, treat the row as suspect. xssFuzz cannot reach those inputs, and the framework should not have run it against them.",
			"Do not read a silent xssFuzz as a clean result. A network failure makes its sendRequest return None, the following substring test raises a TypeError, and the top-level handler prints an error and ends the run, so a partial scan and a clean scan look the same from outside.",
		},
		References: []string{
			"https://github.com/Asperis-Security/xssFuzz",
			"https://portswigger.net/web-security/cross-site-scripting/contexts",
			"https://portswigger.net/web-security/cross-site-scripting/reflected",
			"https://cwe.mitre.org/data/definitions/79.html",
			"https://owasp.org/www-community/attacks/xss/",
		},
	},
	"xssfuzz|R": {
		Title:             "xssFuzz R: the literal string >< came back unencoded somewhere in the response",
		WhatItProved:      "Exactly one thing, and it is small. xssFuzz's detect_characters sets danger_input = \"><randomstring\", substitutes it into the URL with re.sub(f\"{param}=([^&]+)\", ...), issues a single requests.get, and evaluates \"if danger_input in response\". So the finding means the fourteen-character literal ><randomstring appeared, byte for byte, somewhere in the response text. That is real and it is worth something: it establishes that < and > were not HTML-entity-encoded on at least one path through that response.",
		WhatItDidNotProve: "Everything else. There is no HTML parsing, no context analysis, no browser, and no second request. The match is Python's \"in\", a plain substring test against the whole response body, so a hit inside an HTML comment, a JSON string, a JavaScript string literal, a <textarea>, an error page echoing the URL, or a Content-Type that never renders as HTML all count identically. The framework stores this at kind R with confidence \"reflected, dangerous characters unfiltered\" and detection_method \"reflection\" precisely because it is not a vulnerability claim. Two storage facts will otherwise be misread. There is NO payload on the row because the parser does not set one, yet the payload is always the same fixed literal ><randomstring and you need it to reproduce. And the stored url is the vector's evidence URL, NOT the URL xssFuzz fuzzed, so a row can be stored against a bare path with no query string at all, which cannot be the request that produced the finding.",
		WhyItMatters:      "It matters as a cheap triage signal on a parameter no other tool flagged, and as nothing more. The reference target shows exactly why the ceiling is low. Sending category=><rs0nMARKER to https://ginandjuice.shop/catalog returns the marker twice, verified live: HTML-encoded and harmless as value=\"><rs0nMARKER\" in a hidden input, and raw a few lines later inside const selectedCategory = \"><rs0nMARKER\"; . xssFuzz fires on the second. The application encoded the characters in the one place where < and > matter and left them raw in a place where they do not, because inside a double-quoted JS string the characters that matter are \" and </script . The tool's claim is true and its implication is false.",
		FalsePositive:     "Not a false positive in the strict sense, because the substring really was there. The failure is the inference drawn from it. Recurring shapes: the raw hit sits inside a JavaScript string literal where angle brackets are inert, which is the reference case; the raw hit is inside a <textarea> or an HTML comment; the raw hit is in a JSON API response no browser renders; or the encoded copy is the one the page actually uses and the raw copy is a hidden debug field. There is also a mechanical one: re.sub(f\"{param}=([^&]+)\") requires the parameter to already be present with a NON-EMPTY value, so where it is not, xssFuzz sends the URL unchanged and any pre-existing ><randomstring on the page reports a parameter that was never touched. A second mechanical one: the parameter name is interpolated into that regex unescaped, so a name containing regex metacharacters substitutes somewhere unintended or not at all.",
		SeverityNote:      "Medium as stored is generous. Treat it as low or informational until a browser shows execution. Reflection of < and > in an unspecified context is a lead, not a finding. Reporting this as XSS on the strength of the tool's exclamation mark is how a report gets closed as informative.",
		ValidationSteps: []string{
			"Reconstruct the request by hand, because the stored row does not hold it. The probe string is always the literal ><randomstring : curl -sG \"https://TARGET/PATH\" --data-urlencode \"PARAM=><rs0nMARKER\" -o r.html ; grep -n \"rs0nMARKER\" r.html",
			"Expect several hits. Look at each one and write down its context. Only hits in an HTML text node or an unquoted attribute position make < and > meaningful.",
			"Discard hits where the marker is entity-encoded, hits inside a <script> string literal, hits inside <textarea>, <title> or a comment, and hits in a response whose Content-Type is not text/html.",
			"If any hit survives, test the characters THAT context needs, not the ones xssFuzz tested. A quoted attribute needs its quote. A quoted JS string needs that quote and </script . HTML text already needs < and >, which you have.",
			"Only then build a payload and load it in a browser with DevTools open.",
			"Negative control that tells you whether the tool learned anything: send PARAM=rs0nMARKER with no >< prefix. If the marker reflects in exactly the same places, the finding added no information beyond \"this parameter reflects\".",
			"Do not expect xssFuzz to have made an execution claim elsewhere in the same run. Its -V stage prints \"[+] XSS Validated at <url>\" and \"[+] XSS Validated on <url>\", which the framework's parser does not currently match, so a browser-backed row from this tool will not appear in the modal even when -V was set. Treat the R as the only xssFuzz evidence you have and do your own browser step.",
		},
		References: []string{
			"https://github.com/Asperis-Security/xssFuzz",
			"https://portswigger.net/web-security/cross-site-scripting/contexts",
			"https://portswigger.net/web-security/cross-site-scripting/cheat-sheet",
			"https://cwe.mitre.org/data/definitions/79.html",
			"https://owasp.org/www-community/attacks/xss/",
		},
	},
}
